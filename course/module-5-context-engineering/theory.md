# Управление контекстом (Context Engineering)

## Проблема потери контекста

AI-агент не имеет памяти между сессиями. Каждый раз, когда вы открываете новый чат с Claude или запускаете Claude Code, агент начинает с нуля — он не помнит ничего из предыдущих разговоров. Контекстное окно современных моделей ограничено (128K-200K токенов для Claude Opus 4.6), и всё, что не помещается в окно, для агента не существует. Без документированных решений каждая новая сессия превращается в археологические раскопки: агент читает код, пытается угадать архитектуру, делает неверные предположения и переизобретает уже принятые решения.

**Context Engineering** — это управление тем, какая информация попадает в контекстное окно агента на каждом шаге работы. Вы определяете, что загружать в начале (instructions, memories, конституция проекта), что добавлять по ходу (результаты поиска, файлы, tool outputs), что сжимать или удалять (summarization, trimming старых сообщений). Плохо организованный проект заставляет агента тратить драгоценные токены на поиск информации и делать неверные предположения. Хорошо организованный проект — это когда агент быстро находит нужное, следует установленным конвенциям и не задаёт лишних вопросов.

Решение проблемы потери контекста — **артефакты**: файлы, которые хранят контекст между сессиями. Это не просто документация для людей — это память агента, его справочник, его должностная инструкция.

## Spec-Driven Development

**SDD (Spec-Driven Development)** — методология разработки с AI-агентами, при которой вы сначала пишете детальную спецификацию задачи, а затем агент реализует её автономно. Вместо размытого "сделай фичу аутентификации" вы пишете чёткое техническое задание: какие endpoints, какие параметры, какие ошибки обрабатывать, какие тесты написать, какие паттерны использовать.

Почему это работает? AI-модели обучены на миллиардах примеров кода из GitHub, Stack Overflow, документации. Они отлично умеют переводить спецификацию в код, если спецификация чёткая. Проблема не в том, что модель не может написать код — проблема в том, что она не знает, *какой* код вы хотите. SDD решает эту проблему: вы явно говорите, что нужно, а агент разбирается, как это сделать.

Процесс выглядит так: пишете спецификацию → даёте агенту → агент реализует → вы делаете ревью → итерация до готовности → фиксация решений в артефактах. Спецификация может быть короткой (10-15 строк для простого endpoint) или длинной (несколько страниц для сложного модуля). Главное — чёткость: acceptance criteria, ограничения, примеры использования, тестовые сценарии.

Пример спецификации для endpoint:

```markdown
## POST /api/auth/login

**Цель:** Аутентификация пользователя по email и паролю.

**Request:**
- Body: `{ "email": string, "password": string }`
- Validation: email валиден, password не пустой

**Response:**
- 200: `{ "token": string, "user": { "id": string, "email": string } }`
- 401: `{ "error": "Invalid credentials" }`
- 400: `{ "error": "Validation failed", "fields": [...] }`

**Паттерны:**
- Использовать bcrypt для проверки пароля
- JWT токен с expiry 24h
- Rate limiting: 5 попыток за 15 минут

**Тесты:**
- Успешный логин с валидными credentials
- Ошибка 401 при неверном пароле
- Ошибка 400 при невалидном email
- Rate limiting после 5 неудачных попыток
```

Реальный кейс: в феврале 2026 года Anthropic провела эксперимент ([источник](https://www.anthropic.com/engineering/building-c-compiler)) — 16 параллельных агентов Claude Opus 4.6 написали полноценный C-компилятор на Rust с нуля. За 2000 сессий и $20,000 API-запросов они создали 100,000 строк кода, который компилирует Linux 6.9 для x86, ARM и RISC-V. Компилятор проходит 99% GCC torture test suite и даже компилирует игру Doom. Ключевой фактор успеха — **test-driven development**: агенты работали в жёстком feedback loop: пишут код → запускают тесты → анализируют ошибки → итерируют. Без чётких тестов как спецификации агенты решали бы не ту проблему.

### Автономный промпт как спецификация

Пример из проекта **Enji Fleet** (внутренний проект Mad Devs, используемый как учебный пример) — автономный промпт для агента-разработчика. Это не просто инструкция — это полный pipeline из 10 шагов с блокирующими правилами и Progress Table.

**Pipeline:**
1. Настройка окружения (установка инструментов без вопросов)
2. Чтение constitution, decision log, entry.md
3. Создание изолированного git worktree
4. Поиск текущей задачи в Progress Table (статусы: 🔄 in progress, ⬜ not started, ✅ done, ❌ failed)
5. Обновление статуса → 🔄
6. Реализация кода + тесты (TDD)
7. Верификация: goimports, go vet, go test, go build
8. Обновление статуса → ✅ или ❌
9. Заполнение trace файла (инкрементально)
10. Создание PR

**Блокирующие правила:**
> **СТОП и исправь:** тест падает, go vet ошибки, coverage < минимума, build падает
> **ЗАПРЕЩЕНО:** переходить к следующей задаче при ошибках, код без тестов

**Режим работы:**
> **Действуй автономно.** Не спрашивай разрешения на создание файлов, установку инструментов, запуск тестов, исправление ошибок.

Это пример того, как промпт становится "должностной инструкцией" для агента. Чёткие правила, статусы, блокирующие условия — агент знает, что делать на каждом шаге.

## Артефакты SDD

Артефакты — это не просто документы. Это иерархия, где каждый уровень решает свою задачу. Рассмотрим трёхуровневую систему, используемую в проектах с AI-агентами.

### 1. Constitution (Конституция проекта)

**Что это:** живой документ ~400-500 строк, обязательный к прочтению перед любой работой. Constitution — это набор правил, принципов и ограничений для AI-агента, определяющих что можно и нельзя делать в проекте. Содержит tech stack, архитектурные решения (ADR), обязательные паттерны и запреты (antipatterns). Это "память" агента, которой у него нет от природы.

**Где хранить:** `AGENTS.md`, `CLAUDE.md` в корне проекта, или `constitution.md` в директории документации. Большинство AI coding agents автоматически читают AGENTS.md в корне репозитория.

**Шаблон:**

```markdown
# Project Constitution

## Tech Stack
- Backend: Go 1.22, Echo v4, PostgreSQL 16
- Frontend: React 18, TypeScript 5, Vite

## Обязательные паттерны
- Все API handlers возвращают `Result<T, APIError>`
- Используем Repository pattern для доступа к БД
- Тесты: table-driven tests

## Antipatterns (запрещено)
- Прямые SQL-запросы в handlers (только через repositories)
- panic() в production коде
- Изменение миграций после merge в main
```

### 2. Feature Specification (Спецификация задачи)

**Что это:** детальное описание конкретной задачи с acceptance criteria, ограничениями, примерами. Plan (план) — документ с подзадачами, последовательностью шагов и критериями приёмки для выполнения фичи.

**Где хранить:** `plans/` или `specs/` директория. Обычно нумерованные планы типа `003-auth-system.plan.md`, которые выполняются строго по порядку.

**Шаблон:**

```markdown
# Plan 003: Authentication System

## Цель
Реализовать JWT-аутентификацию для API.

## Acceptance Criteria
- [ ] POST /api/auth/login возвращает JWT токен
- [ ] Middleware проверяет токен на protected routes
- [ ] Токен истекает через 24h
- [ ] Rate limiting 5 попыток / 15 минут

## Ограничения
- Использовать bcrypt для паролей
- JWT с HS256, секрет из ENV
- Следовать constitution: Repository pattern

## Implementation Notes
**Решения:** используем sliding window с Redis для rate limiting.
**Альтернативы:** Fixed window проще, но позволяет 10 попыток на границе окна.

## Примеры
Login request: `{ "email": "user@example.com", "password": "secret" }`
```

### 3. Trace (История реализации)

**Что это:** документ, который агент создаёт после выполнения плана. Trace (трейс) — это лог действий агента, фиксирующий принятые решения, отклонения от плана, проблемы и их решения.

**Где хранить:** `traces/` директория. Файлы типа `003-auth-system.trace.md` создаются после завершения соответствующего плана.

**Шаблон:**

```markdown
# Trace: Authentication System

## Что сделано
Реализованы endpoints /login, /refresh, middleware для JWT.

## Для конституции
- Добавить: все auth endpoints требуют rate limiting
- Паттерн: JWT claims должны включать `user_id` и `role`

## Технические решения
- Выбрали HS256 вместо RS256: проще для старта, достаточно для MVP
- Храним refresh token в БД: нужна возможность revoke

## Проблемы и решения
- **Проблема:** тесты падали из-за race condition в token validation
- **Решение:** добавили mutex в token cache
```

### Цикл накопления знаний

Артефакты образуют замкнутый цикл накопления знаний:

```
Agent работает → пишет trace → консолидация знаний →
обновляет constitution → следующий agent читает constitution
```

**Reflect-mode** — это специальный режим работы агента-ревьюера, который:
- НЕ пишет код (нет доступа к инструментам разработки)
- Читает traces от других агентов
- Консолидирует знания в constitution
- Проверяет соответствие кода паттернам из constitution
- Выявляет повторяющиеся проблемы и добавляет их в antipatterns

Это защищает главный артефакт (constitution) от хаотичных изменений. Агенты-разработчики не могут случайно его испортить — они только создают traces с предложениями. Reflect-mode принимает финальное решение о том, что попадёт в constitution.

## Специальные артефакты

### AGENTS.md / CLAUDE.md

Это "README для агентов" — файл в корне проекта, который агенты читают автоматически. Содержит описание проекта, архитектуры, команды для запуска, конвенции кода. Ключевое отличие от README: AGENTS.md содержит детальный контекст для агентов (build steps, test conventions, паттерны), который захламлял бы обычный README.

**Важно:** не превращайте AGENTS.md в замену линтеру. Code style проверяется автоматически (prettier/eslint). Пишите только то, что не проверяется автоматически.

Примеры: [Next.js AGENTS.md](https://github.com/vercel/next.js/blob/canary/AGENTS.md), [Anthropic CLAUDE.md](https://github.com/anthropics/claude-code-action/blob/main/CLAUDE.md).

### Memory Bank

Контекст, который агент должен помнить между сессиями. В Claude Code есть встроенная система memories — агент может сохранять и читать заметки о проекте.

**Memory Bank паттерн:** директория `.claude/memories/` или `.cline/memory_bank/`, где каждый файл — отдельный аспект проекта. Агент читает релевантные файлы по запросу, экономя контекст.

**Пример структуры:**
```
.claude/memories/
  ├── auth.md          # Аутентификация и авторизация
  ├── database.md      # Схема БД, миграции
  ├── api-design.md    # Конвенции API, error handling
  └── testing.md       # Стратегия тестирования
```

Когда агент работает с аутентификацией, он читает `auth.md`. Когда работает с БД — `database.md`. Это экономит контекст по сравнению с загрузкой всей constitution.

## Skills

**Skills** — это готовые промпты и подходы, заточенные под конкретные повторяющиеся задачи. Вместо того, чтобы каждый раз писать "сделай code review, обрати внимание на X, Y, Z", вы создаёте skill `/review` и вызываете его одной командой.

В Claude Code skills — это markdown-файлы с front matter, которые хранятся в `~/.claude/skills/` (глобально) или `project/.claude/skills/` (для конкретного проекта). Skills всегда сохраняются в файле `SKILL.md`, а имя skill определяется именем папки.

### Примеры skills

**Code review:**
```markdown
---
skill: review
description: Проверка кода на качество, безопасность, соответствие конвенциям
---

# Code Review Checklist

1. **Функциональность:** код делает то, что должен?
2. **Тесты:** покрытие тестами, edge cases
3. **Безопасность:** SQL injection, XSS, CSRF
4. **Производительность:** N+1 queries, избыточные запросы
5. **Читаемость:** названия переменных, комментарии
6. **Паттерны:** соответствие constitution
```

**Тесты:**
```markdown
---
skill: test
description: Написание тестов с полным покрытием
---

# Test Writing Guide

**Coverage:** minimum 80% для новых файлов.
**Edge cases:** пустые входы, null, максимальные значения.
**Конвенции:** table-driven tests для Go, describe/it для JS.
```

**SDD workflow:**
```markdown
---
skill: sdd
description: Spec-driven development процесс
---

# SDD Workflow

1. Прочитай constitution
2. Прочитай feature specification
3. Напиши тесты (TDD)
4. Реализуй минимальный код для прохождения тестов
5. Запусти линтеры и тесты
6. Создай trace файл
7. Создай PR
```

### Готовые решения

- **[cc-sdd](https://github.com/gotalab/cc-sdd):** полный SDD-workflow для Claude Code — requirements → design → tasks → implementation
- **[GitHub Spec Kit](https://github.com/github/spec-kit):** официальный тулкит от GitHub — конституция, спеки, планы, задачи с чеклистами
- **[claude-skills-starter](https://github.com/angakh/claude-skills-starter):** 12 готовых slash-команд — `/commit`, `/quality`, `/test`, `/scaffold`

### Как создать skill

1. Создайте директорию для skill: `.claude/skills/my-skill/`
2. Создайте файл `SKILL.md` в этой директории
3. Добавьте front matter с описанием:
```markdown
---
skill: my-skill
description: Краткое описание того, что делает skill
---
```
4. Опишите процесс или чеклист в markdown
5. Вызывайте: `/my-skill` в Claude Code

Skills позволяют автоматизировать повторяющиеся workflow и специализировать агентов на конкретные задачи: разработка, документация, ревью, тестирование. Каждый skill — это готовый промпт с чёткими инструкциями. Вместо того, чтобы каждый раз объяснять агенту, что делать, вы просто вызываете соответствующий skill.

## Организация контекста в проекте

Ключевые практики для правильной организации контекста:

- **README:** высокоуровневое описание проекта, как запустить, как контрибьютить
- **AGENTS.md / CLAUDE.md:** детальный контекст для агентов — архитектура, build steps, конвенции
- **docs/:** отдельные документы для сложных систем — architecture.md, api.md, deployment.md
- **Чистая структура папок:** агент не должен гадать, где что лежит

**Важно:** контекстное окно ограничено. Информация должна быть компактной и релевантной. Не пишите 10-страничные доклады — пишите структурированные чеклисты и примеры.

### Правила бюджета контекста

**Бюджет constitution:** ориентир ~400-500 строк, но главное — полезность. Каждая строка должна помогать агенту принять правильное решение.

**Что включать:**
- Tech stack с точными версиями (Go 1.22, PostgreSQL 16)
- Обязательные паттерны (Repository pattern, error handling)
- Antipatterns — что категорически запрещено
- Примеры кода для типичных задач

**Что НЕ включать:**
- Code style guidelines (используйте линтер)
- Полную документацию API (ссылка на Swagger достаточно)
- Tutorial "как работать с Git" (агент это знает)

**Структурирование для быстрого поиска:**

Агенты используют grep и семантический поиск для навигации по документам. Помогите им:
- Используйте чёткие заголовки (## Authentication, ## Database Patterns)
- Выделяйте ключевые слова жирным (**ОБЯЗАТЕЛЬНО**, **ЗАПРЕЩЕНО**)
- Группируйте связанную информацию в списки и таблицы
- Добавляйте примеры кода рядом с правилами

Агент не читает документы линейно — он ищет релевантные секции. Чем легче информацию найти, тем эффективнее работает агент.

**Memory bank паттерн:** директория с тематическими файлами (auth.md, database.md, deployment.md), которые агент читает по необходимости. Это компромисс: полная конституция не помещается в контекст, а memory bank позволяет загружать только нужное. Примеры: [Cline Memory Bank](https://docs.cline.bot/prompting/cline-memory-bank), Claude Code memories.

## Версионирование артефактов

Артефакты — это код проекта. Они живут в git, и их история изменений — это история принятых решений.

**Практики:**
- Все артефакты в git: constitution, plans, traces
- Промпты, которые дали хороший результат, сохраняются в отдельной директории (`prompts/`)
- Изменения в constitution через pull requests с ревью
- Trace файлы создаются вместе с PR, который они описывают

История артефактов в git — это живой учебник: почему мы выбрали PostgreSQL, почему отказались от microservices, какие ошибки делали и как их исправили. Следующий агент может прочитать эту историю и не повторять ошибок.

## От AGENTS.md к полноценной системе артефактов

В Модуле 4 мы познакомились с AGENTS.md — единым файлом с инструкциями для агентов. Это отличная отправная точка, но для больших проектов её недостаточно. Система артефактов — это эволюция AGENTS.md.

**Эволюция подхода:**

**Уровень 1:** Нет документации. Агент гадает по коду.

**Уровень 2:** AGENTS.md в корне. Агент знает основы: tech stack, как запустить проект, базовые конвенции. Подходит для pet-проектов и MVP.

**Уровень 3:** AGENTS.md + Memory Bank. Агент имеет доступ к тематическим файлам. Подходит для средних проектов (5-10K строк кода).

**Уровень 4:** Constitution + Plans + Traces. Полноценная иерархия артефактов. Агент знает не только "как", но и "почему". Подходит для больших проектов.

Не нужно сразу прыгать на уровень 4. Начните с AGENTS.md, добавляйте артефакты по мере роста проекта. Сложность должна соответствовать масштабу задачи.

## Практические советы

**Как начать с SDD:**

1. **Напишите AGENTS.md** с минимальным контекстом: tech stack, как запустить проект, основные правила.
2. **Создайте первую спецификацию** для следующей фичи. Потратьте 15 минут на чёткое описание вместо 2 часов на объяснение агенту.
3. **Заведите директорию для traces** и просите агента создавать trace после каждой задачи. Это ваша история решений.
4. **Когда traces накопятся** (5-10 файлов), выделите время на консолидацию: какие паттерны повторяются? Что стоит добавить в AGENTS.md?
5. **Версионируйте всё.** Артефакты в git = история мышления проекта.

**Чего избегать:**

- **Не дублируйте код в документации.** Ссылайтесь на примеры в кодовой базе вместо копирования.
- **Не пишите романы.** 400 строк полезного контекста лучше 4000 строк воды.
- **Не превращайте AGENTS.md в линтер.** Code style проверяется автоматически, не тратьте на это контекст.
- **Не забывайте обновлять.** Устаревшая документация хуже её отсутствия — агент будет делать неверные предположения.

## Резюме

**Context Engineering** — это управление тем, что видит агент. **Spec-Driven Development** — это процесс: спецификация → реализация → ревью → фиксация знаний. **Артефакты** (Constitution, Specification, Trace) — это иерархия знаний проекта. **Skills** — это переиспользуемые промпты для повторяющихся задач. Всё это превращает AI-агента в полноценного члена команды, который помнит контекст и следует правилам.

---

## Дополнительный материал

Для углублённого изучения контекст-инжиниринга на реальном примере смотрите: [Context Engineering на практике: обязательные артефакты агентов](./context-engineering-practices.md) — детальный разбор системы артефактов из проекта Enji Fleet (планы, трейсы, фейлы, конституция, блокирующие правила).

## Источники

- [Building a C compiler with a team of parallel Claudes](https://www.anthropic.com/engineering/building-c-compiler) — Anthropic
- [Understanding Spec-Driven-Development: Kiro, spec-kit, and Tessl](https://martinfowler.com/articles/exploring-gen-ai/sdd-3-tools.html) — Martin Fowler
- [AGENTS.md specification](https://agents.md/) — открытый формат для инструкций AI-агентам
- [Creating the Perfect CLAUDE.md for Claude Code](https://dometrain.com/blog/creating-the-perfect-claudemd-for-claude-code/) — Dometrain
- [GitHub Spec Kit announcement](https://github.blog/ai-and-ml/generative-ai/spec-driven-development-with-ai-get-started-with-a-new-open-source-toolkit/) — GitHub Blog
