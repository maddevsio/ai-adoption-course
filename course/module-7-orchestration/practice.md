# Упражнения по оркестрации

5 практических упражнений, которые проведут вас от git worktrees до параллельной работы нескольких агентов. Каждое упражнение строится на предыдущем. Суммарно 60 минут + 15 минут бонусные упражнения.

**Требования перед началом:**
- Установленный Claude Code (через `curl -fsSL https://claude.ai/install.sh | bash`)
- Git репозиторий с проектом (можете использовать свой или создать тестовый)
- Две открытые сессии терминала или две вкладки в IDE

**Важно:** Эти упражнения практические. Не читайте их как текст — выполняйте каждый шаг. Именно практика даёт понимание того, как работает оркестрация.

---

## Шаг 1: Git worktree для изоляции агентов (10 минут)

Git worktree создаёт отдельную рабочую директорию для каждой ветки. Это позволяет двум агентам работать параллельно без конфликтов.

### Упражнение: создать worktree и запустить агента

**1. Посмотрите текущие worktrees:**

```bash
git worktree list
```

Вы увидите только основную директорию. Это нормально — у вас пока нет дополнительных worktrees.

**2. Создайте новый worktree для фичи:**

```bash
# Создать worktree в директории на уровень выше
git worktree add ../myproject-add-logging -b feat/add-logging

# Проверить что worktree создан
git worktree list
```

Вывод покажет две директории:
```
/home/user/myproject        (branch: main)
/home/user/myproject-add-logging  (branch: feat/add-logging)
```

**3. Перейдите в worktree:**

```bash
cd ../myproject-add-logging
ls -la
```

Вы увидите те же файлы, что и в основной директории, но это изолированная копия. Изменения здесь не влияют на основную директорию до момента merge.

**4. Запустите агента в worktree:**

```bash
claude
```

Дайте агенту задачу:
```
Добавь базовое логирование в проект:
1. Настрой logger с форматом "[timestamp] [level] message"
2. Добавь логирование в 2-3 ключевых местах (например, при запуске приложения)
3. Запусти проверку что код работает
```

Агент работает только в директории worktree. Основная директория остаётся нетронутой.

**5. После завершения работы агента:**

```bash
# Посмотрите что изменилось
git status
git diff

# Вернитесь в основную директорию
cd ../myproject

# Проверьте что основная директория чистая
git status
# Output: nothing to commit, working tree clean
```

**6. Удалите worktree после merge:**

```bash
# Если ветка замержена
git worktree remove ../myproject-add-logging

# Если нужно удалить worktree принудительно (например, агент упал)
git worktree remove --force ../myproject-add-logging
```

### Правило из Enji Fleet

В проекте **Enji Fleet** (учебный пример Mad Devs для демонстрации best practices) действует жёсткое правило: **никогда не работать в основной директории**. Каждый план выполняется в отдельном worktree с паттерном именования `../enji-fleet-<plan-name>`.

Одна ветка = один план = один worktree. Это предотвращает 90% конфликтов при параллельной работе.

**Чек-лист:**
- [ ] Вы создали worktree
- [ ] Запустили агента в worktree
- [ ] Убедились что основная директория осталась чистой
- [ ] Удалили worktree после завершения

**Зачем это нужно:** Worktrees позволяют запускать несколько агентов параллельно. Пока один агент работает над логированием в worktree A, другой может работать над API endpoints в worktree B. Они не мешают друг другу.

---

## Шаг 2: Генератор + Ревьюер (15 минут)

Паттерн: один агент пишет код, другой проверяет. Это простейшая форма оркестрации для повышения качества.

### Упражнение: два агента работают последовательно

**Контекст задачи:** Нужно добавить DELETE endpoint в REST API.

**1. Подготовьте два терминала:**
- Терминал 1: Агент-генератор
- Терминал 2: Агент-ревьюер

**2. В Терминале 1 запустите агента-генератора:**

```bash
# Создайте worktree для фичи
git worktree add ../myproject-delete-endpoint -b feat/delete-endpoint
cd ../myproject-delete-endpoint

# Запустите Claude Code
claude
```

**Промпт для генератора:**

```
Задача: Добавить DELETE /api/tasks/{id} endpoint

Требования:
1. Endpoint должен удалять задачу по ID
2. Вернуть 204 No Content при успехе
3. Вернуть 404 Not Found если задача не существует
4. Добавить тесты: успешное удаление, попытка удалить несуществующую задачу
5. Запустить все тесты проекта

Действуй автономно. Не спрашивай подтверждения.
```

Агент напишет код и создаст файлы. Дождитесь завершения.

**3. В Терминале 2 запустите агента-ревьюера:**

```bash
# Ревьюер работает в той же директории
cd ../myproject-delete-endpoint

# Запустите второго агента (новую сессию)
claude
```

**Промпт для ревьюера:**

```
Проведи code review кода в текущей директории.

Проверь следующие критерии:

1. **Тесты:**
   - Все edge cases покрыты?
   - Тесты проходят?
   - Coverage достаточный?

2. **Стиль кода:**
   - Код следует принятому стилю проекта?
   - Нейминг переменных понятный?
   - Есть комментарии где нужно?

3. **Безопасность:**
   - Проверяется ли авторизация?
   - Нет ли SQL-инъекций?
   - Валидируется ли input?

4. **Документация:**
   - API endpoint задокументирован?
   - Есть docstrings для новых функций?

Создай отчёт в формате:

## Найденные проблемы
[список проблем с конкретными местами в коде]

## Рекомендации
[что нужно исправить]

## Что сделано хорошо
[позитивные моменты]
```

Ревьюер проанализирует код и создаст отчёт.

**4. Передайте результат ревью обратно генератору:**

Вернитесь в Терминал 1 (генератор ещё работает) и дайте новый промпт:

```
Вот результат code review:

[скопируйте сюда отчёт ревьюера]

Исправь все найденные проблемы.
```

Генератор внесёт исправления.

**5. Повторите цикл ревью (опционально):**

Если проблемы были серьёзные, можете запустить ревьюера ещё раз после исправлений.

**6. Рефлексия:**

Ответьте себе на вопросы:
- Нашёл ли ревьюер проблемы, которые вы бы не заметили?
- Какие типы ошибок он отловил? (тесты, безопасность, стиль, документация)
- Стоила ли дополнительная проверка затраченного времени и токенов?

**Чек-лист:**
- [ ] Генератор написал код
- [ ] Ревьюер проверил код и создал отчёт
- [ ] Генератор исправил найденные проблемы
- [ ] Вы провели рефлексию

**Когда использовать паттерн:**
- Критичный код (безопасность, платежи, работа с персональными данными)
- Незнакомая технология (первый раз используете фреймворк)
- Код для production (всё что идёт в релиз)

**Когда НЕ использовать:**
- Простые задачи (добавить поле в форму, исправить опечатку)
- Прототипы и эксперименты
- Код который точно будет переписан

---

## Шаг 3: Параллельная работа двух агентов (15 минут)

Два агента выполняют разные задачи одновременно. Это ускоряет разработку в 2 раза.

### Упражнение: два worktrees, два агента, две задачи

**Контекст:** Нужно добавить две независимые фичи в проект.

**1. Создайте два worktrees:**

```bash
# Worktree A для работы с пользователями
git worktree add ../myproject-users-api -b feat/users-api

# Worktree B для логирования запросов
git worktree add ../myproject-request-logging -b feat/request-logging

# Проверьте что оба созданы
git worktree list
```

**2. Откройте два терминала:**

**Терминал A (Агент A):**
```bash
cd ../myproject-users-api
claude
```

**Промпт для Агента A:**
```
Добавь GET /api/users endpoint для получения списка пользователей.

Требования:
1. Endpoint возвращает массив пользователей
2. Фильтровать удалённых пользователей (deleted_at = null)
3. Поддержка pagination: query params ?page=1&limit=20
4. Добавить тесты: получение списка, pagination, фильтрация удалённых
5. Запустить все тесты

Действуй автономно.
```

**Терминал B (Агент B):**
```bash
cd ../myproject-request-logging
claude
```

**Промпт для Агента B:**
```
Добавь middleware для логирования HTTP-запросов.

Требования:
1. Логировать каждый входящий запрос: метод, путь, IP, timestamp
2. Логировать время выполнения запроса
3. Не логировать health-check endpoints (/health, /ping)
4. Формат лога: [2025-01-15 10:30:45] GET /api/users from 192.168.1.10 - 45ms
5. Добавить тесты для middleware
6. Запустить все тесты

Действуй автономно.
```

**3. Мониторинг агентов:**

Переключайтесь между терминалами и наблюдайте за прогрессом. Оба агента работают независимо.

**Что может пойти не так:**
- Один агент может застрять на падающем тесте → Ralph Loop исправит
- Агент может запросить дополнительную информацию → дайте её в той же сессии
- Тесты могут конфликтовать (например, оба меняют один тестовый файл) → это нормально, разрешите при merge

**4. Дождитесь завершения обоих агентов.**

**5. Проверьте результаты:**

```bash
# В worktree A
cd ../myproject-users-api
git status
git log -1  # Посмотреть последний коммит

# В worktree B
cd ../myproject-request-logging
git status
git log -1
```

**Чек-лист:**
- [ ] Оба worktrees созданы
- [ ] Оба агента запущены параллельно
- [ ] Агент A завершил задачу с users endpoint
- [ ] Агент B завершил задачу с logging middleware
- [ ] Вы проверили что изменения изолированы

**Замер времени:**

Если бы вы делали обе задачи последовательно одним агентом, это заняло бы 30-40 минут. Параллельно — 15-20 минут. Ускорение в 2 раза.

**Рефлексия:**
- Было ли сложно мониторить обе сессии одновременно?
- Возникли ли конфликты между агентами?
- Стоило ли ускорение затраченных усилий?

---

## Шаг 4: Слияние результатов (10 минут)

После параллельной работы нужно объединить изменения двух агентов в одну ветку.

### Упражнение: merge двух веток и разрешение конфликтов

**1. Вернитесь в основную директорию:**

```bash
cd ../myproject  # или как называется ваш проект
git checkout main
```

**2. Смержите первую ветку:**

```bash
# Merge users-api
git merge feat/users-api

# Проверьте что всё хорошо
git log --oneline -3
```

Первый merge обычно проходит гладко, конфликтов нет.

**3. Смержите вторую ветку:**

```bash
# Merge request-logging
git merge feat/request-logging
```

**Если возникли конфликты:**

Git покажет файлы с конфликтами:
```
CONFLICT (content): Merge conflict in tests/test_api.py
Automatic merge failed; fix conflicts and then commit the result.
```

**4. Разрешите конфликты:**

Откройте файл с конфликтом в редакторе:
```bash
code tests/test_api.py  # или vim, nano
```

Найдите секцию:
```python
<<<<<<< HEAD
# Код из feat/users-api
def test_users_endpoint():
    ...
=======
# Код из feat/request-logging
def test_logging_middleware():
    ...
>>>>>>> feat/request-logging
```

**Три способа разрешения:**

**Способ 1: Ручное объединение (самый надёжный)**
```python
# Оставьте оба теста
def test_users_endpoint():
    ...

def test_logging_middleware():
    ...
```

**Способ 2: Агент-арбитр**

Запустите нового агента:
```bash
claude
```

Промпт:
```
В файле tests/test_api.py возник merge conflict между двумя ветками.

Ветка feat/users-api добавила test_users_endpoint.
Ветка feat/request-logging добавила test_logging_middleware.

Разреши конфликт: оставь оба теста, убедись что импорты корректные, форматирование единообразное.
```

**Способ 3: Принять одну из сторон**
```bash
# Принять нашу версию (HEAD)
git checkout --ours tests/test_api.py

# Или принять их версию (feat/request-logging)
git checkout --theirs tests/test_api.py
```

Используется редко — только если точно знаете что одна версия правильная.

**5. После разрешения конфликтов:**

```bash
# Добавить разрешённые файлы
git add tests/test_api.py

# Завершить merge
git commit -m "Merge feat/request-logging into main"
```

**6. Запустите тесты на объединённом результате:**

```bash
# Запустите все тесты проекта
pytest  # или npm test, go test, etc.
```

Тесты должны пройти. Если падают — значит конфликт разрешён неправильно или агенты создали несовместимые изменения.

**7. Удалите worktrees:**

```bash
git worktree remove ../myproject-users-api
git worktree remove ../myproject-request-logging

# Опционально: удалите ветки
git branch -d feat/users-api
git branch -d feat/request-logging
```

**Чек-лист:**
- [ ] Обе ветки смержены в main
- [ ] Конфликты разрешены (если были)
- [ ] Тесты проходят на объединённом результате
- [ ] Worktrees удалены

**Рефлексия:**

Ответьте на вопросы:
- Возникли ли конфликты при merge?
- Если да — какого типа? (изменения одного файла, дублирование кода, несовместимые импорты)
- Как вы разрешили конфликты? (вручную, через агента, автоматически)
- Что можно было сделать на этапе планирования, чтобы конфликтов не было?

**Лучшая практика:** Предотвращение конфликтов на этапе планирования. Если два агента должны работать с одним файлом, либо делайте это последовательно, либо разделите файл на модули (например, `users_routes.py` и `logging_middleware.py` вместо одного `app.py`).

---

## Шаг 5: Создайте runbook для своего проекта (10 минут)

Runbook — формализованный план параллельной работы агентов. Это документ, который можно передать оркестратору или использовать самостоятельно для координации агентов.

### Упражнение: написать runbook для фичи из вашего проекта

**1. Выберите задачу из вашего проекта:**

Подходящие задачи:
- Новая фича с backend + frontend + tests
- Рефакторинг с несколькими независимыми компонентами
- Миграция на новую версию библиотеки (обновление кода + тестов + документации)

**Пример:** "Добавить экспорт списка задач в CSV"

**2. Создайте файл runbook:**

```bash
touch RUNBOOK-export-tasks-csv.md
```

**3. Используйте шаблон:**

```markdown
# Runbook: [название задачи]

## Цель
[Что делаем и зачем. Одно-два предложения]

## Подзадачи

### 1. [Название подзадачи]
- **Агент:** [профиль агента, например claude-developer]
- **Worktree:** [название worktree, например ../myproject-backend-export]
- **Ветка:** [название ветки, например feat/backend-export]
- **Критерии приёмки:**
  - [конкретный критерий 1]
  - [конкретный критерий 2]
  - [конкретный критерий 3]

### 2. [Название подзадачи]
- **Агент:** [профиль агента]
- **Worktree:** [название worktree]
- **Ветка:** [название ветки]
- **Критерии приёмки:**
  - [конкретный критерий 1]
  - [конкретный критерий 2]

[Добавьте все подзадачи]

## Порядок выполнения

```
[Задача 1] ──┐
             ├──> [Задача 3]
[Задача 2] ──┘
```

- Задачи 1 и 2 выполняются параллельно
- Задача 3 начинается после завершения 1 и 2

## Процедура слияния

1. Merge [ветка задачи 1] в main
2. Merge [ветка задачи 2] в main
3. Разрешить конфликты в [файлы где могут быть конфликты]
4. Запустить [команды для тестов]
5. Merge [ветка задачи 3] в main

## Ожидаемые риски

- **Конфликт:** Обе задачи могут изменить [файл]. План: [как разрешить]
- **Зависимость:** Задача 3 зависит от API из задачи 1. План: проверить API контракт перед стартом задачи 3
- **[другие риски]**

## Время выполнения

- Последовательно: [X минут]
- Параллельно: [Y минут]
- Ускорение: [X/Y]x
```

**4. Заполните шаблон для вашей задачи:**

**Пример заполненного runbook:**

```markdown
# Runbook: Экспорт списка задач в CSV

## Цель

Добавить возможность экспортировать список задач в CSV-файл через API и UI. Пользователь нажимает кнопку "Export" → скачивается tasks.csv со всеми задачами.

## Подзадачи

### 1. Backend: API endpoint для экспорта
- **Агент:** claude-developer (Sonnet 4.5)
- **Worktree:** ../myproject-backend-export
- **Ветка:** feat/backend-export-csv
- **Критерии приёмки:**
  - Endpoint GET /api/tasks/export возвращает CSV
  - Все поля задачи экспортируются (id, title, status, created_at)
  - Тесты покрывают: успешный экспорт, пустой список, спецсимволы в данных
  - Все тесты проекта проходят

### 2. Frontend: кнопка Export
- **Агент:** claude-developer (Sonnet 4.5)
- **Worktree:** ../myproject-frontend-export
- **Ветка:** feat/frontend-export-button
- **Критерии приёмки:**
  - Кнопка "Export to CSV" в таблице задач
  - Клик по кнопке скачивает tasks.csv
  - Показывается loader во время загрузки
  - Обработаны ошибки (сеть недоступна, сервер вернул 500)
  - UI тесты проходят

### 3. E2E тесты
- **Агент:** claude-developer (Sonnet 4.5)
- **Worktree:** ../myproject-e2e-export
- **Ветка:** feat/e2e-export-tests
- **Критерии приёмки:**
  - E2E тест: создать задачи → нажать Export → проверить содержимое CSV
  - Негативный тест: экспорт при пустом списке
  - Тесты проходят в CI

## Порядок выполнения

```
[Backend] ──┐
            ├──> [E2E тесты]
[Frontend] ─┘
```

- Backend и Frontend выполняются параллельно (независимы)
- E2E тесты начинаются после завершения Backend и Frontend

## Процедура слияния

1. Merge feat/backend-export-csv в main
2. Merge feat/frontend-export-button в main
3. Проверить конфликты в package.json, requirements.txt (если агенты добавили зависимости)
4. Запустить полный тестовый набор: `pytest && npm test`
5. Merge feat/e2e-export-tests в main
6. Запустить E2E тесты: `npm run test:e2e`

## Ожидаемые риски

- **Конфликт:** Оба агента могут изменить package.json (добавить зависимости). План: принять обе зависимости, проверить дубликаты.
- **API контракт:** Frontend зависит от точного формата CSV из Backend. План: прописать формат в runbook (заголовки CSV: id,title,status,created_at).
- **Timing:** E2E тесты могут начаться раньше чем Backend смержен. План: запускать E2E агента только после merge Backend и Frontend.

## Время выполнения

- Последовательно: 60 минут (20 мин backend + 20 мин frontend + 20 мин E2E)
- Параллельно: 35 минут (20 мин backend+frontend параллельно + 10 мин E2E + 5 мин merge)
- Ускорение: 1.7x
```

**5. Сохраните runbook в репозиторий:**

```bash
git add RUNBOOK-export-tasks-csv.md
git commit -m "Add runbook for CSV export feature"
```

**Чек-лист:**
- [ ] Выбрана задача из вашего проекта
- [ ] Задача декомпозирована на 2-3 подзадачи
- [ ] Для каждой подзадачи указан агент, worktree, критерии
- [ ] Определён порядок выполнения (параллельно/последовательно)
- [ ] Описана процедура слияния
- [ ] Перечислены ожидаемые риски
- [ ] Runbook сохранён в репозиторий

### Образец из Enji Fleet

В проекте **Enji Fleet** (демонстрационный проект Mad Devs) есть промпт `docs/prompts/code-implementation-prompt.md` — это runbook на 300 строк для автономного агента-разработчика.

**Что включено:**
- **Progress Table** с emoji-статусами (⬜ pending, 🔄 in progress, ✅ done, ❌ failed)
- **Блокирующие правила:** "СТОП и исправь: тест падает, go vet ошибки, coverage < минимума"
- **Инкрементальный trace:** агент обновляет trace-файл после каждого шага

Пример Progress Table:
```markdown
| Status | Task | Notes |
|--------|------|-------|
| ✅ | Setup worktree | Branch: feat/add-memory-service |
| ✅ | Read constitution | Tech stack: Go 1.24, ChromaDB |
| 🔄 | Implement Memory Service | In progress |
| ⬜ | Write tests | Pending |
| ⬜ | Create PR | Pending |
```

Агент обновляет статус в таблице после каждого шага. Координатор видит прогресс в реальном времени.

**Использование в курсе:** Ваш runbook может включать подобную таблицу для каждого агента. Это помогает мониторить прогресс при параллельной работе нескольких агентов.

### Пример agent-profiles.yaml

Из проекта **Enji Fleet** (учебный пример конфигурации — `backend/services/agent/agent-profiles.yaml`):

```yaml
profiles:
  claude-developer:
    cli: claude
    roles: [developer, reviewer, tester, deployer, researcher]
    model: claude-sonnet-4-5-20250929
    system_prompt: "You are a senior software developer."
    max_turns: 100
    priority: normal

  claude-architect:
    cli: claude
    roles: [planner, reviewer, researcher, evaluator]
    model: claude-sonnet-4-5-20250929
    system_prompt: "You are a software architect."
    max_turns: 50
    priority: high

  codex-developer:
    cli: codex
    roles: [developer, tester]
    model: codex-mini
    priority: low
```

**Как использовать в runbook:**

В разделе "Подзадачи" указывайте профиль агента:
- `claude-architect` для планирования и проектирования
- `claude-developer` для основной разработки
- `codex-developer` для простых и дешёвых задач

Task Service оркестратора подбирает агента по роли. Если нужен `planner` → выбирается `claude-architect` с high priority.

---

## Бонус: Расширенные возможности (15 минут, опционально)

Эта секция содержит 5 расширенных тем. Прочитайте обзор каждой и выберите одну для практики.

### 1. Claude Code headless: автоматизация через CLI

Claude Code может работать без интерактивного интерфейса — в headless-режиме.

**Зачем это нужно:**
- Запускать агентов в CI/CD пайплайне
- Автоматизировать рутинные задачи (генерация отчётов, рефакторинг)
- Масштабировать на несколько серверов (не ограничиваться вашим ноутбуком)

**Базовое использование:**

```bash
# Запустить агента с промптом
claude -p "Добавь type hints во все функции в src/utils.py"

# Запустить агента с промптом из файла
claude -p prompts/fix-linting-errors.md

# Вывод в JSON для парсинга скриптами
claude -p "Опиши архитектуру проекта" --output-format json > architecture.json
```

**Упражнение: создать скрипт для автоматического рефакторинга**

1. Создайте файл промпта:

```bash
cat > prompts/add-docstrings.md << 'EOF'
Добавь docstrings ко всем публичным функциям и классам в проекте.

Формат docstring (Google style):
```python
def function_name(param1: str, param2: int) -> bool:
    """Brief description.

    Args:
        param1: Description of param1
        param2: Description of param2

    Returns:
        Description of return value
    """
```

Не трогай функции которые уже имеют docstrings.
Запусти проверку: `pydocstyle src/`
EOF
```

2. Запустите агента:

```bash
claude -p prompts/add-docstrings.md
```

3. Используйте в CI/CD (GitHub Actions):

```yaml
name: Auto-add docstrings

on:
  schedule:
    - cron: '0 2 * * 1'  # Каждый понедельник в 2:00

jobs:
  add-docstrings:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Install Claude Code
        run: curl -fsSL https://claude.ai/install.sh | bash

      - name: Run agent
        uses: anthropics/claude-code-action@v1
        with:
          prompt-file: prompts/add-docstrings.md
          anthropic-api-key: ${{ secrets.ANTHROPIC_API_KEY }}

      - name: Create PR
        run: |
          git checkout -b auto/add-docstrings-$(date +%Y%m%d)
          git add .
          git commit -m "Auto-add docstrings"
          git push origin auto/add-docstrings-$(date +%Y%m%d)
```

Каждый понедельник в 2:00 агент проверяет проект, добавляет docstrings где их нет, и создаёт PR.

**Где ещё использовать:**
- Автоматическая генерация changelog перед релизом
- Обновление зависимостей (агент анализирует breaking changes и обновляет код)
- Автоматическое исправление security issues из Dependabot

---

### 2. Auth-токены для автоматизации

Для headless-режима нужны токены, которые не требуют интерактивного логина.

**Два типа токенов:**

**API Key (для API-биллинга):**

```bash
# Получить токен в console.anthropic.com/settings/keys
export ANTHROPIC_API_KEY=sk-ant-api03-...

# Использовать в скриптах
claude -p "Generate unit tests" --api-key $ANTHROPIC_API_KEY
```

Биллинг: pay-as-you-go, $15 за 1M tokens (Claude Opus), $3 за 1M tokens (Claude Sonnet).

**Альтернатива: Claude Max подписка**

Для интенсивного использования можно оформить подписку Claude Max ($100/мес или $200/мес для Max 20x) и использовать тот же API key через console.anthropic.com.

**Упражнение: настроить headless-агента с API key**

1. Получите API key через console.anthropic.com/settings/keys

2. Создайте `.env` файл для локальной автоматизации:
```bash
cat > .env << EOF
ANTHROPIC_API_KEY=sk-ant-api03-ваш_ключ
EOF
```

3. Используйте в скриптах:
```bash
#!/bin/bash
source .env

# Запустить агента для всех сервисов
for service in api-gateway user-service payment-service; do
  echo "Processing $service..."
  cd services/$service
  claude -p "Update to Go 1.24, run tests"
  cd ../..
done
```

**Безопасность:**

- Добавьте `.env` в `.gitignore` (никогда не коммитьте токены)
- В CI/CD используйте secrets:
  - GitHub: Settings → Secrets → ANTHROPIC_API_KEY
  - GitLab: Settings → CI/CD → Variables
- Для production: используйте secrets managers (AWS Secrets Manager, HashiCorp Vault)

---

### 3. Claude Agent SDK: программная оркестрация

SDK позволяет управлять агентами из кода (Python или TypeScript), а не через CLI.

**Когда использовать SDK вместо CLI:**
- Нужен сложный workflow (агент A → агент B → агент C с условиями)
- Интеграция с внешними системами (Jira, Slack, Sentry)
- Кастомные хуки (отправить уведомление когда агент завершил задачу)
- Динамическая генерация промптов

**Установка:**

```bash
# Python
pip install anthropic

# TypeScript
npm install @anthropic-ai/sdk
```

**Упражнение: создать оркестратор на Python**

1. Создайте файл `orchestrator.py`:

```python
from anthropic import Anthropic
import os

# Конфигурация
client = Anthropic(api_key=os.getenv("ANTHROPIC_API_KEY"))

# Задачи
tasks = [
    {
        "name": "add-logging",
        "prompt": "Add logging to all API endpoints with format [timestamp] [level] [method] [path]"
    },
    {
        "name": "add-rate-limiting",
        "prompt": "Add rate limiting middleware: 100 requests per minute per IP"
    }
]

# Запустить агента для каждой задачи
for task in tasks:
    print(f"Starting task: {task['name']}")

    # Создать сообщение
    message = client.messages.create(
        model="claude-sonnet-4-5-20250929",
        max_tokens=4096,
        system="You are a senior Python developer.",
        messages=[
            {"role": "user", "content": task['prompt']}
        ]
    )

    # Обработать результат
    result = message.content[0].text
    print(f"Task {task['name']}: {result[:100]}...")
```

2. Запустите оркестратор:

```bash
export ANTHROPIC_API_KEY=sk-ant-api03-...
python orchestrator.py
```

**Примечание:** Для полноценной оркестрации агентов с возможностью работы с файлами и выполнения команд используйте Claude Code CLI в headless-режиме или специализированные платформы (OpenClaw).

Агент автоматически исправляет баги по мере их поступления в Sentry.

**Когда использовать SDK:**
- Интеграция с task trackers (Jira, Linear, GitHub Issues)
- Event-driven automation (новый PR → агент проводит review)
- Custom orchestration (10 агентов на 10 микросервисах одновременно)

**Когда хватает `claude -p`:**
- Разовые задачи
- Простые workflows (один агент, одна задача)
- Эксперименты и прототипы

---

### 4. Agent Teams: экспериментальная координация агентов

Agent Teams — это экспериментальная концепция координации нескольких агентов в одной команде.

**Статус (Feb 2026):**

Agent Teams включается через environment variable и является экспериментальной функцией:

```bash
# Включить экспериментальный режим
export CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1
claude

# Проверить что feature включена:
# Claude покажет "[Agent Teams Mode]" в статусе при запуске
```

**Важно:** Команда `claude teams start` не существует в публичной версии Claude Code. Agent Teams работает как расширенный режим основной команды `claude`.

**Возможности:**

1. **Shared task list** — все агенты видят общую очередь задач и берут следующую свободную:

```
Shared Task Queue:
- [ ] Add login endpoint
- [ ] Add registration endpoint
- [ ] Add password reset endpoint
- [ ] Write E2E tests

Agent 1: берёт "Add login endpoint"
Agent 2: берёт "Add registration endpoint"
Agent 3: берёт "Add password reset endpoint"
```

2. **Inter-agent messaging** — агенты могут запрашивать информацию друг у друга:

```
Agent 2 → Agent 1: "Какой формат JWT токена ты используешь в login endpoint?"
Agent 1 → Agent 2: "{ alg: HS256, payload: { user_id, exp } }"
```

3. **Plan approval mode** — агенты предлагают план, человек одобряет, агенты исполняют автономно:

```
Agents: "Мы разделили задачу на 4 подзадачи. План:
1. Agent A: database migrations
2. Agent B: API endpoints
3. Agent C: frontend components
4. Agent D: integration tests
Начать выполнение?"

You: "Одобрено. Начинайте."
```

**Упражнение: попробовать Agent Teams на простой задаче**

1. Включите экспериментальный режим:

```bash
export CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1
```

2. Создайте task list файл `tasks.md`:

```markdown
# Task List

- [ ] Add GET /api/products endpoint
- [ ] Add POST /api/products endpoint
- [ ] Add unit tests for products endpoints
- [ ] Add API documentation for products
```

3. Запустите Claude в экспериментальном режиме:

```bash
# Agent Teams включается через env var, не через CLI флаг
CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1 claude

# В интерактивном режиме Claude может координировать выполнение задач
# из task list, но конкретный API может отличаться от описанного
```

**Важное примечание:** Команда `claude teams start` не существует в публичной версии. Agent Teams работает как расширенный режим, который включается через environment variable. Точный API и возможности могут меняться, так как это экспериментальная функция.

**Статус (Feb 2026):** Экспериментальный. API может измениться. Не рекомендуется для production.

**Реальный кейс от Anthropic:**

Команда Anthropic запустила 16 параллельных агентов для работы над Rust-компилятором проекта C. За примерно 2000 сессий агенты написали и отрефакторили около 100K строк кода. Большинство PR были приняты после минимального review.

**Проблемы:**
- Координация требует значительного overhead (агенты общаются → больше токенов)
- Межагентное общение может создавать циклические зависимости (Agent A ждёт Agent B, который ждёт Agent A)
- Стоимость 16 параллельных Sonnet сессий высокая ($200-300/день)

**Когда использовать:**
- Экспериментальные проекты с большим бюджетом
- Крупные рефакторинги (миграция кодовой базы на новый язык)
- Research проекты

**Когда НЕ использовать:**
- Production проекты (пока experimental)
- Малый бюджет
- Простые задачи (overhead не окупится)

---

### 5. OpenClaw: платформа автономных агентов

**OpenClaw** — open-source платформа для координации автономных агентов. Разработана Питером Штайнбергером.

**GitHub:** https://github.com/openclaw/openclaw (188K stars)

**Что это:**

OpenClaw — не инструмент для написания кода напрямую, а платформа для координации агентов, которые работают с разными системами: код, Slack, Jira, email, браузер.

**Ключевые возможности:**

1. **Multi-agent routing** — разные модели на разные задачи:
   - Claude Opus для сложного кода
   - GPT-4 для анализа текста и документации
   - Gemini Flash для быстрых и дешёвых операций
   - Codex для простого кодинга

2. **Интеграция с Claude Code через MCP:**
   - OpenClaw вызывает Claude Code как один из инструментов
   - MCP протокол для коммуникации между платформой и агентами

3. **Экосистема оркестраторов:**
   - **Claworc** — управление инстансами агентов, масштабирование на сотни workers
   - **Antfarm** — team-of-agents паттерн, агенты совместно решают задачу

**Пример workflow:**

```
1. Пользователь пишет в Slack: "fix bug PROJ-123"
2. OpenClaw Slack-бот читает сообщение
3. OpenClaw берёт задачу из Jira (PROJ-123)
4. OpenClaw запускает Claude Code для исправления бага
5. Claude Code создаёт PR
6. OpenClaw отправляет результат в Slack: "Bug fixed in PR #456"
```

**Когда использовать OpenClaw:**

- Нужна координация агентов через разные системы (не только код)
- Компания хочет построить полноценную автономную систему разработки
- Есть инфраструктура для запуска платформы (серверы, Kubernetes)
- Команда готова поддерживать open-source проект

**Когда НЕ использовать:**

- Индивидуальный разработчик (слишком сложно)
- Простые задачи (написать код) — хватает Claude Code напрямую
- Нет инфраструктуры для развёртывания платформы

**Упражнение: изучить архитектуру OpenClaw (10 минут, чтение)**

1. Прочитайте README: https://github.com/openclaw/openclaw

2. Ключевые концепции:
   - **Agent Pool** — пул доступных агентов (Claude, GPT, Gemini, Codex)
   - **Router** — маршрутизатор задач к нужному агенту по типу задачи
   - **Integration Layer** — интеграция с Slack, Jira, GitHub, email
   - **MCP Bridge** — мост между OpenClaw и Claude Code

3. Архитектура:

```
Slack/Jira/Email
       ↓
   OpenClaw Core (Router)
       ↓
   ┌───┴───┬───────┬─────────┐
   ↓       ↓       ↓         ↓
Claude   GPT-4  Gemini   Codex
 Code
```

4. Посмотрите примеры конфигурации:

```yaml
# openclaw.yaml
agents:
  claude-code:
    type: mcp
    binary: claude
    roles: [code, review, refactor]
    priority: high
    cost_per_1m_tokens: 3.0

  gpt4:
    type: api
    endpoint: https://api.openai.com/v1
    roles: [analysis, documentation]
    priority: normal
    cost_per_1m_tokens: 10.0

  gemini-flash:
    type: api
    endpoint: https://generativelanguage.googleapis.com
    roles: [search, simple-tasks]
    priority: low
    cost_per_1m_tokens: 0.5

routing:
  - task_type: code
    agent: claude-code
  - task_type: documentation
    agent: gpt4
  - task_type: simple
    agent: gemini-flash
```

**Для кого это:**

- Компании с 50+ разработчиками
- Команды которые хотят полностью автоматизировать рутинные процессы
- Open-source проекты с большим количеством contributors

**Установка (опционально, если есть время):**

```bash
# Клонировать репозиторий
git clone https://github.com/openclaw/openclaw.git
cd openclaw

# Установить зависимости
npm install

# Настроить конфигурацию
cp config.example.yaml config.yaml
# Отредактировать config.yaml: добавить API ключи, настроить агентов

# Запустить платформу
npm start
```

Платформа запустит веб-интерфейс на http://localhost:3000 где можно:
- Мониторить работу агентов
- Создавать задачи вручную
- Просматривать логи и метрики
- Настраивать routing между агентами

---

## Итоги упражнений

Вы прошли путь от базовой изоляции через git worktrees до расширенных техник оркестрации.

**Что вы освоили:**

1. **Git worktree** — изоляция агентов для параллельной работы
2. **Генератор + Ревьюер** — двухэтапная проверка для повышения качества
3. **Параллельная работа** — два агента на независимых задачах
4. **Слияние результатов** — merge веток и разрешение конфликтов
5. **Runbook** — формализованный план для оркестрации агентов

**Бонусные темы:**

- **Headless-режим** — автоматизация через CLI и CI/CD
- **Auth-токены** — долговременные токены для автоматизации
- **Agent SDK** — программная оркестрация на Python/TypeScript
- **Agent Teams** — экспериментальная координация агентов
- **OpenClaw** — платформа для enterprise-уровня автоматизации

**Следующие шаги:**

1. **Практикуйте на реальных проектах.** Возьмите фичу из вашего проекта и реализуйте её с использованием 2-3 агентов параллельно.

2. **Создайте библиотеку runbook'ов.** Для типовых задач (добавить endpoint, создать CRUD, написать тесты) создайте готовые runbook'и.

3. **Экспериментируйте с профилями агентов.** Попробуйте разные модели (Sonnet vs Haiku vs Opus) на разных типах задач. Измеряйте время и стоимость.

4. **Автоматизируйте рутину.** Настройте headless-агентов для регулярных задач (обновление зависимостей, генерация документации, исправление linting errors).

5. **Изучите теорию оркестрации.** Прочитайте Модуль 7 (theory.md) для понимания концепций: Ralph Loop, ролевая модель, паттерны оркестрации, мониторинг.

**Рефлексия:**

Ответьте себе на вопросы:
- Какие упражнения были самыми полезными?
- Где вы можете применить параллельную работу агентов в ваших проектах?
- Какие проблемы возникли и как вы их решили?
- Стоит ли overhead на координацию затраченных усилий?

**Помните:** Оркестрация — это не о замене разработчика агентами. Это о переходе от роли "я пишу код" к роли "я проектирую систему, которая пишет код". Вы становитесь архитектором оркестра, менеджером процессов, контролёром качества.

Первое время это сложнее и медленнее. Но когда система настроена, вы получаете производительность команды из 5-10 человек, работая в одиночку или с небольшой командой.

---

## Troubleshooting: Распространённые проблемы

### Проблема: Agent Teams не работает

**Симптомы:**
- Флаг `--agent-teams` не распознаётся
- Ошибка "unknown flag: --agent-teams"
- Команда `claude teams start` не найдена

**Решение:**

Agent Teams в Feb 2026 использует environment variable, не CLI флаг:

```bash
# Правильный способ включения Agent Teams
export CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1
claude

# Или одной строкой:
CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1 claude "your prompt"

# Проверить что feature включена:
# Claude покажет "[Agent Teams Mode]" в статусе при запуске
```

**Важно:**
- Команда `claude teams start --agents N` не существует в публичной версии
- Agent Teams — экспериментальная функция, API может измениться
- Не все возможности из документации могут быть доступны

**Альтернативы для параллельной работы:**

Если Agent Teams не работает или недоступна, используйте проверенные подходы:

1. **Git worktrees** (описано в Шаге 1):
   ```bash
   git worktree add ../project-task1 -b feat/task1
   git worktree add ../project-task2 -b feat/task2
   # Запустите claude в каждом worktree в отдельном терминале
   ```

2. **Программная оркестрация через SDK** (описано в разделе Бонус 3):
   ```python
   from anthropic import Anthropic
   # Запуск нескольких задач программно
   ```

3. **Внешние платформы** (OpenClaw, описано в разделе Бонус 5):
   - Для enterprise-решений
   - Требует инфраструктуры

### Проблема: OAuth токен не работает

**Симптомы:**
- Ошибка "authentication failed"
- Команда `claude setup-token` не найдена

**Решение:**

Команда `claude setup-token` не существует. Токены получаются через веб-интерфейс:

```bash
# 1. Получите токен на https://claude.ai/settings/developer
# 2. Сохраните в файл:
mkdir -p ~/.claude
cat > ~/.claude/auth.json << EOF
{"access_token": "your-token-here"}
EOF

# 3. Используйте в скриптах:
export ANTHROPIC_API_KEY="sk-ant-api03-YOUR_KEY_HERE"
# или
export CLAUDE_CODE_OAUTH_TOKEN=$(cat ~/.claude/auth.json | jq -r '.access_token')
```

### Проблема: Worktree конфликты при merge

**Симптомы:**
- Git показывает CONFLICT при merge
- Тесты падают после объединения веток

**Решение:**

1. **Разрешите конфликты вручную:**
   ```bash
   # Откройте файл с конфликтом
   code path/to/conflicted/file.py

   # Найдите секции с <<<<<<< HEAD
   # Объедините изменения вручную
   # Сохраните файл

   git add path/to/conflicted/file.py
   git commit -m "Resolve merge conflicts"
   ```

2. **Или используйте агента для разрешения:**
   ```bash
   claude
   # Промпт: "Разреши merge conflict в file.py.
   # Оставь изменения из обеих веток, убедись что код корректный."
   ```

3. **Предотвращение конфликтов:**
   - Планируйте задачи так, чтобы агенты работали с разными файлами
   - Разделяйте большие файлы на модули
   - Используйте clear separation of concerns

### Проблема: Агент застрял в бесконечном цикле

**Симптомы:**
- Агент повторяет одно и то же действие
- Тесты постоянно падают и агент пытается исправить

**Решение:**

1. **Прервите агента:** Ctrl+C
2. **Проанализируйте логи:** Что пошло не так?
3. **Дайте более конкретный промпт:**
   ```
   Вместо: "Исправь тесты"
   Используйте: "Тест test_user_login падает с ошибкой KeyError: 'token'.
   Проверь что функция login() возвращает словарь с ключом 'token'."
   ```

### Проблема: Headless режим не работает в CI/CD

**Симптомы:**
- GitHub Actions job падает
- Ошибка "ANTHROPIC_API_KEY not found"

**Решение:**

1. **Добавьте secret в GitHub:**
   - Settings → Secrets and variables → Actions
   - New repository secret: `ANTHROPIC_API_KEY`

2. **Используйте secret в workflow:**
   ```yaml
   - name: Run Claude Code
     env:
       ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
     run: |
       npm install -g @anthropic-ai/claude-code
       claude -p "your prompt"
   ```

3. **Проверьте permissions:**
   - Workflow должен иметь доступ к secrets
   - Branch protection rules могут блокировать некоторые actions