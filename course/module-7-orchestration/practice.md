# Упражнения по оркестрации

5 практических упражнений, которые проведут вас от git worktrees до параллельной работы нескольких агентов. Каждое упражнение строится на предыдущем. Суммарно 60 минут + 15 минут бонусные упражнения.

## Чек-лист готовности

Перед началом убедитесь что:
- [ ] Claude Code установлен (Модуль 2)
- [ ] Git установлен и настроен
- [ ] Вы знакомы с MCP (Модуль 6)
- [ ] Есть 60-75 минут для упражнений
- [ ] Две вкладки терминала или IDE открыты

**Требования перед началом:**
- Установленный Claude Code (через `curl -fsSL https://claude.ai/install.sh | bash`)
- Git репозиторий с проектом (можете использовать свой или создать тестовый)
- Две открытые сессии терминала или две вкладки в IDE

**Важно:** Эти упражнения практические. Не читайте их как текст — выполняйте каждый шаг. Именно практика даёт понимание того, как работает оркестрация.

---

## Шаг 1: Git worktree для изоляции агентов (10 минут)

Git worktree создаёт отдельную рабочую директорию для каждой ветки, позволяя агентам работать параллельно без конфликтов.

### Подготовка

Создайте тестовый репозиторий или используйте существующий:

```bash
mkdir ~/practice/test-orchestration && cd ~/practice/test-orchestration
git init
echo "# Test Orchestration Project" > README.md
git add README.md && git commit -m "Initial commit"
```

### Упражнение: создать worktree и запустить агента

**1. Посмотрите текущие worktrees:**

```bash
git worktree list
```

Вы увидите только основную директорию. Это нормально — у вас пока нет дополнительных worktrees.

**2. Создайте новый worktree для фичи:**

```bash
git worktree add ../myproject-add-logging -b feat/add-logging
git worktree list
```

**3. Перейдите в worktree:**

```bash
cd ../myproject-add-logging
```

Изменения в worktree изолированы от основной директории до момента merge.

**4. Запустите агента в worktree:**

```bash
claude
```

Дайте агенту задачу:
```
Добавь базовое логирование в проект:
1. Настрой logger с форматом "[timestamp] [level] message"
2. Добавь логирование в 2-3 ключевых местах (например, при запуске приложения)
3. Запусти проверку что код работает
```

Агент работает только в директории worktree. Основная директория остаётся нетронутой.

**5. После завершения работы агента:**

```bash
git status  # проверить изменения
cd ../myproject  # вернуться в основную директорию
git status  # убедиться что основная директория чистая
```

**6. Удалите worktree после merge:**

```bash
git worktree remove ../myproject-add-logging
# Или принудительно: git worktree remove --force ../myproject-add-logging
```

**Чек-лист:**
- [ ] Вы создали worktree
- [ ] Запустили агента в worktree
- [ ] Убедились что основная директория осталась чистой
- [ ] Удалили worktree после завершения

**Принцип:** Одна ветка = один план = один worktree. Это предотвращает конфликты при параллельной работе.

---

## Шаг 2: Генератор + Ревьюер (15 минут)

Паттерн: один агент пишет код, другой проверяет. Это простейшая форма оркестрации для повышения качества.

### Упражнение: два агента работают последовательно

**Контекст задачи:** Нужно добавить DELETE endpoint в REST API.

**1. Подготовьте два терминала:**
- Терминал 1: Агент-генератор
- Терминал 2: Агент-ревьюер

**2. В Терминале 1 запустите агента-генератора:**

```bash
# Создайте worktree для фичи
git worktree add ../myproject-delete-endpoint -b feat/delete-endpoint
cd ../myproject-delete-endpoint

# Запустите Claude Code
claude
```

**Промпт для генератора:**

```
Задача: Добавить DELETE /api/tasks/{id} endpoint

Требования:
1. Endpoint должен удалять задачу по ID
2. Вернуть 204 No Content при успехе
3. Вернуть 404 Not Found если задача не существует
4. Добавить тесты: успешное удаление, попытка удалить несуществующую задачу
5. Запустить все тесты проекта

Действуй автономно. Не спрашивай подтверждения.
```

Агент напишет код и создаст файлы. Дождитесь завершения.

**3. В Терминале 2 запустите агента-ревьюера:**

```bash
# Ревьюер работает в той же директории
cd ../myproject-delete-endpoint

# Запустите второго агента (новую сессию)
claude
```

**Промпт для ревьюера:**

```
Проведи code review кода в текущей директории.

Проверь следующие критерии:

1. **Тесты:**
   - Все edge cases покрыты?
   - Тесты проходят?
   - Coverage достаточный?

2. **Стиль кода:**
   - Код следует принятому стилю проекта?
   - Нейминг переменных понятный?
   - Есть комментарии где нужно?

3. **Безопасность:**
   - Проверяется ли авторизация?
   - Нет ли SQL-инъекций?
   - Валидируется ли input?

4. **Документация:**
   - API endpoint задокументирован?
   - Есть docstrings для новых функций?

Создай отчёт в формате:

## Найденные проблемы
[список проблем с конкретными местами в коде]

## Рекомендации
[что нужно исправить]

## Что сделано хорошо
[позитивные моменты]
```

Ревьюер проанализирует код и создаст отчёт.

**4. Передайте результат ревью обратно генератору:**

Вернитесь в Терминал 1 (генератор ещё работает) и дайте новый промпт:

```
Вот результат code review:

[скопируйте сюда отчёт ревьюера]

Исправь все найденные проблемы.
```

Генератор внесёт исправления.

**5. Повторите цикл ревью (опционально):**

Если проблемы были серьёзные, можете запустить ревьюера ещё раз после исправлений.

**Пример реального отчёта ревьюера:**

```markdown
## Найденные проблемы

### 1. Безопасность
- Строка 45: DELETE endpoint не проверяет авторизацию
  Риск: любой может удалить любую задачу
  Решение: добавить проверку user_id == task.owner_id

### 2. Тесты
- Отсутствует тест для попытки удаления чужой задачи
  Решение: добавить test_delete_task_unauthorized

### 3. Документация
- Не задокументирован status code 403
  Решение: добавить в docstring

## Рекомендации

1. Rate limiting для DELETE (предотвращение abuse)
2. Логировать операции удаления (audit trail)
3. Soft delete вместо hard delete

## Что сделано хорошо

✅ Правильная обработка 404
✅ Тесты покрывают happy path
✅ Код читаемый
```

**6. Генератор исправляет:**

```bash
# В терминале генератора дайте промпт:
Исправь проблемы из review:
1. Добавь проверку авторизации в DELETE endpoint
2. Добавь test_delete_task_unauthorized
3. Обнови docstring с 403 status code
4. Запусти все тесты
```

**7. Рефлексия:**

Ответьте себе на вопросы:
- Нашёл ли ревьюер проблемы, которые вы бы не заметили?
- Какие типы ошибок он отловил? (тесты, безопасность, стиль, документация)
- Стоила ли дополнительная проверка затраченного времени и токенов?

**Чек-лист:**
- [ ] Генератор написал код
- [ ] Ревьюер проверил код и создал отчёт
- [ ] Генератор исправил найденные проблемы
- [ ] Вы провели рефлексию

**Анализ результатов:**

После завершения упражнения ответьте на вопросы:
- Какие проблемы нашёл ревьюер? (тесты, безопасность, стиль, документация)
- Были ли проблемы, которые вы бы не заметили самостоятельно?
- Стоила ли двойная проверка затраченного времени и токенов?

**Когда использовать паттерн:**
- Критичный код (безопасность, платежи, персональные данные)
- Незнакомая технология (первый раз используете фреймворк)
- Production код (всё что идёт в релиз)
- Сложная бизнес-логика с edge cases

**Когда НЕ использовать:**
- Простые задачи (добавить поле, исправить опечатку)
- Прототипы и эксперименты
- Код который будет переписан
- Внутренние инструменты для разработки

**Стоимость паттерна:**

Генератор + Ревьюер удваивает использование токенов, но:
- Снижает количество багов на 40-60%
- Улучшает качество кода (style, документация)
- Экономит время на debugging

Для критичного кода это оправдано.

**Вариации паттерна:**

**1. Генератор + Несколько специализированных ревьюеров:**
- Security reviewer (проверяет безопасность)
- Performance reviewer (проверяет производительность)
- Code style reviewer (проверяет стиль)

Каждый фокусируется на своей области.

**2. Ping-pong паттерн:**
Генератор → Ревьюер → Генератор → Ревьюер (повторять до идеального кода)

**3. Peer review паттерн:**
Два агента пишут решения независимо, третий выбирает лучшее или комбинирует.

---

## Шаг 3: Параллельная работа двух агентов (15 минут)

Два агента выполняют разные задачи одновременно. Это ускоряет разработку в 2 раза.

### Упражнение: два worktrees, два агента, две задачи

**Контекст:** Нужно добавить две независимые фичи в проект.

**1. Создайте два worktrees:**

```bash
# Worktree A для работы с пользователями
git worktree add ../myproject-users-api -b feat/users-api

# Worktree B для логирования запросов
git worktree add ../myproject-request-logging -b feat/request-logging

# Проверьте что оба созданы
git worktree list
```

**2. Откройте два терминала:**

**Терминал A (Агент A):**
```bash
cd ../myproject-users-api
claude
```

**Промпт для Агента A:**
```
Добавь GET /api/users endpoint для получения списка пользователей.

Требования:
1. Endpoint возвращает массив пользователей
2. Фильтровать удалённых пользователей (deleted_at = null)
3. Поддержка pagination: query params ?page=1&limit=20
4. Добавить тесты: получение списка, pagination, фильтрация удалённых
5. Запустить все тесты

Действуй автономно.
```

**Терминал B (Агент B):**
```bash
cd ../myproject-request-logging
claude
```

**Промпт для Агента B:**
```
Добавь middleware для логирования HTTP-запросов.

Требования:
1. Логировать каждый входящий запрос: метод, путь, IP, timestamp
2. Логировать время выполнения запроса
3. Не логировать health-check endpoints (/health, /ping)
4. Формат лога: [2025-01-15 10:30:45] GET /api/users from 192.168.1.10 - 45ms
5. Добавить тесты для middleware
6. Запустить все тесты

Действуй автономно.
```

**3. Мониторинг:**

Переключайтесь между терминалами и наблюдайте за прогрессом.

**4. Дождитесь завершения обоих агентов.**

**5. Проверьте результаты:**

```bash
cd ../myproject-users-api && git status
cd ../myproject-request-logging && git status
```

**Чек-лист:**
- [ ] Оба worktrees созданы
- [ ] Оба агента запущены параллельно
- [ ] Обе задачи завершены
- [ ] Изменения изолированы

**Результат:** Ускорение в 2 раза по сравнению с последовательным выполнением.

**Измерение эффективности:**

Запишите метрики:
- Время начала: [timestamp]
- Время завершения каждого агента: Agent A [timestamp], Agent B [timestamp]
- Время merge: [timestamp]
- Общее время: [XX минут]

Сравните с последовательным выполнением:
- Последовательно: Agent A (15 мин) + Agent B (15 мин) = 30 мин
- Параллельно: max(15, 15) + merge (5 мин) = 20 мин
- Экономия: 10 мин (33%)

**Рефлексия:**
- Сложно ли было мониторить обе сессии?
- Возникли ли конфликты между агентами?
- Какие типы задач лучше делать параллельно?

**Оптимальные задачи для параллелизации:**

✅ Хорошо параллелятся:
- Backend + Frontend (разные технологии, разные файлы)
- API endpoints для разных ресурсов (/users, /posts)
- Независимые микросервисы
- Тесты + Документация

❌ Плохо параллелятся:
- Две фичи изменяющие один файл
- Зависимые задачи (Feature B использует API из Feature A)
- Миграции базы данных (должны быть последовательными)

---

## Шаг 4: Слияние результатов (10 минут)

После параллельной работы нужно объединить изменения двух агентов в одну ветку.

### Упражнение: merge двух веток и разрешение конфликтов

**1. Вернитесь в основную директорию:**

```bash
cd ../myproject  # или как называется ваш проект
git checkout main
```

**2. Смержите обе ветки:**

```bash
git merge feat/users-api
git merge feat/request-logging
```

**Если возникли конфликты:**

```
CONFLICT (content): Merge conflict in tests/test_api.py
```

**4. Разрешите конфликты:**

**Способ 1: Вручную**
```bash
code tests/test_api.py  # объедините изменения
git add tests/test_api.py
```

**Способ 2: Через агента**
```bash
claude
# Промпт: "Разреши merge conflict в tests/test_api.py.
# Оставь оба теста, проверь импорты и форматирование."
```

**Способ 3: Принять одну сторону (редко используется)**
```bash
git checkout --ours tests/test_api.py  # принять нашу версию
git checkout --theirs tests/test_api.py  # принять их версию
```

Используйте только если точно знаете что одна версия правильная.

**5. Завершите merge и проверьте:**

```bash
git commit -m "Merge feat/request-logging into main"
pytest  # или npm test, go test
```

**6. Удалите worktrees:**

```bash
git worktree remove ../myproject-users-api
git worktree remove ../myproject-request-logging
git branch -d feat/users-api feat/request-logging
```

**Чек-лист:**
- [ ] Обе ветки смержены
- [ ] Конфликты разрешены
- [ ] Тесты проходят
- [ ] Worktrees удалены

**Лучшая практика:** Предотвращайте конфликты на этапе планирования — разделяйте работу по разным файлам/модулям.

**Рефлексия:**
- Какого типа конфликты возникли (если были)?
  - Изменения в одном файле (разные части)
  - Дублирование кода (оба добавили похожие функции)
  - Несовместимые импорты или зависимости
- Как вы их разрешили (вручную/через агента)?
- Что можно было сделать на этапе планирования для предотвращения?

**Типичные конфликты и решения:**

| Тип конфликта | Как предотвратить |
|--------------|-------------------|
| Оба изменили app.py | Разбить на модули: users.py, logging.py |
| Оба добавили зависимости | Указать в runbook какой агент за какие зависимости отвечает |
| Тесты в одном файле | test_users.py и test_logging.py раздельно |
| Конфликт в конфигах | YAML/JSON: разделить на секции, каждому агенту свою |

---

## Шаг 5: Создайте runbook для своего проекта (10 минут)

Runbook — формализованный план параллельной работы агентов. Это документ, который можно передать оркестратору или использовать самостоятельно для координации агентов.

### Упражнение: написать runbook для фичи из вашего проекта

**1. Выберите задачу из вашего проекта:**

Подходящие задачи:
- Новая фича с backend + frontend + tests
- Рефакторинг с несколькими независимыми компонентами
- Миграция на новую версию библиотеки (обновление кода + тестов + документации)

**Пример:** "Добавить экспорт списка задач в CSV"

**2. Создайте файл runbook:**

```bash
touch RUNBOOK-export-tasks-csv.md
```

**3. Используйте шаблон:**

```markdown
# Runbook: [название задачи]

## Цель
[Что делаем и зачем]

## Подзадачи

### 1. [Название]
- **Worktree:** ../myproject-[название]
- **Ветка:** feat/[название]
- **Критерии приёмки:**
  - [критерий 1]
  - [критерий 2]

[Повторите для всех подзадач]

## Порядок выполнения
[Задача 1] ──┐
             ├──> [Задача 3]
[Задача 2] ──┘

## Процедура слияния
1. Merge веток в main
2. Разрешить конфликты
3. Запустить тесты

## Риски
- [Риск 1]: [план разрешения]
```

**4. Заполните шаблон для вашей задачи:**

**Пример заполненного runbook для демонстрации:**

```markdown
# Runbook: Экспорт списка задач в CSV

(Пример для задачи: добавить возможность экспортировать список задач в CSV-файл)

## Цель

Добавить возможность экспортировать список задач в CSV через API и UI. Пользователь нажимает "Export" → скачивается tasks.csv.

## Подзадачи

### 1. Backend: API endpoint
- **Worktree:** ../myproject-backend-export
- **Ветка:** feat/backend-export-csv
- **Критерии:**
  - GET /api/tasks/export возвращает CSV
  - Поля: id, title, status, created_at
  - Тесты: успешный экспорт, пустой список, спецсимволы
  - Все тесты проходят

### 2. Frontend: кнопка Export
- **Worktree:** ../myproject-frontend-export
- **Ветка:** feat/frontend-export-button
- **Критерии:**
  - Кнопка "Export to CSV" в таблице
  - Скачивание tasks.csv
  - Loader во время загрузки
  - Обработка ошибок (сеть, 500)
  - UI тесты проходят

### 3. E2E тесты
- **Worktree:** ../myproject-e2e-export
- **Ветка:** feat/e2e-export-tests
- **Критерии:**
  - E2E: создать задачи → Export → проверить CSV
  - Негативный тест: пустой список
  - CI проходит

## Порядок выполнения

```
[Backend] ──┐
            ├──> [E2E тесты]
[Frontend] ─┘
```

- Backend и Frontend выполняются параллельно (независимы)
- E2E тесты начинаются после завершения Backend и Frontend

## Процедура слияния

1. Merge backend → main
2. Merge frontend → main
3. Проверить конфликты в package.json, requirements.txt
4. Запустить: `pytest && npm test`
5. Merge e2e → main
6. Запустить: `npm run test:e2e`

## Ожидаемые риски

- **Конфликт:** package.json (зависимости). План: объединить, проверить дубликаты.
- **API контракт:** Формат CSV (id,title,status,created_at) должен совпадать в backend и frontend.
- **Timing:** E2E только после merge backend+frontend.

## Время выполнения

- Последовательно: 60 мин (20+20+20)
- Параллельно: 35 мин (20 параллельно + 10 E2E + 5 merge)
- Ускорение: 1.7x

## Примечания

- Формат CSV зафиксирован: `id,title,status,created_at`
- Encoding: UTF-8 with BOM для Excel совместимости
- E2E запускать только после backend готов
```

**5. Сохраните runbook в репозиторий:**

```bash
git add RUNBOOK-export-tasks-csv.md
git commit -m "Add runbook for CSV export feature"
```

**6. Использование runbook:**

Теперь вы (или другой разработчик) можете выполнить задачу следуя runbook:

```bash
# Шаг 1: Прочитать runbook
cat RUNBOOK-export-tasks-csv.md

# Шаг 2: Создать worktrees согласно runbook
git worktree add ../myproject-backend-export -b feat/backend-export-csv
git worktree add ../myproject-frontend-export -b feat/frontend-export-button

# Шаг 3: Запустить агентов параллельно (2 терминала)
# Терминал 1:
cd ../myproject-backend-export && claude
# [дать промпт из runbook для backend]

# Терминал 2:
cd ../myproject-frontend-export && claude
# [дать промпт из runbook для frontend]

# Шаг 4: После завершения обоих — merge согласно процедуре в runbook
cd ../myproject
git merge feat/backend-export-csv
git merge feat/frontend-export-button
pytest && npm test

# Шаг 5: E2E тесты
git worktree add ../myproject-e2e-export -b feat/e2e-export-tests
cd ../myproject-e2e-export && claude
# [дать промпт из runbook для e2e]
```

**Чек-лист:**
- [ ] Выбрана задача из вашего проекта
- [ ] Задача декомпозирована на 2-3 подзадачи
- [ ] Для каждой подзадачи указан агент, worktree, критерии
- [ ] Определён порядок выполнения (параллельно/последовательно)
- [ ] Описана процедура слияния
- [ ] Перечислены ожидаемые риски
- [ ] Runbook сохранён в репозиторий

**Зачем это нужно:**

Runbook формализует план оркестрации и позволяет:
- Воспроизвести workflow в будущем (та же задача через 6 месяцев)
- Передать задачу другому разработчику (онбординг нового члена команды)
- Использовать с программными оркестраторами (SDK, OpenClaw)
- Избежать типовых ошибок (конфликты, пропущенные зависимости, race conditions)
- Оценить время и стоимость заранее

**Шаблон vs Ad-hoc:**

Без runbook: вы импровизируете, забываете про зависимости, получаете конфликты, тратите время на debugging.

С runbook: чёткий план, все риски учтены, агенты работают параллельно без пересечений, merge проходит гладко.

**Библиотека runbook'ов для типовых задач:**

Создайте коллекцию runbook'ов для частых паттернов в вашем проекте:

```
project/runbooks/
  CRUD-endpoint.md          # Добавить CRUD для нового ресурса
  frontend-feature.md       # Добавить frontend фичу (component + state + tests)
  database-migration.md     # Миграция БД (schema + data + rollback)
  api-integration.md        # Интеграция с внешним API
  refactoring-module.md     # Рефакторинг модуля (extract + test + migrate)
```

Пример CRUD-endpoint.md:
```markdown
# Runbook Template: CRUD Endpoint

## Inputs
- Resource name: [RESOURCE]
- Fields: [FIELD1, FIELD2, ...]

## Tasks
1. Backend model + migrations
2. Backend CRUD endpoints
3. Backend tests
4. Frontend API client
5. Frontend CRUD UI
6. E2E tests

[Детальные инструкции для каждой задачи...]
```

Использование:
```bash
# Скопировать шаблон
cp runbooks/CRUD-endpoint.md RUNBOOK-products-api.md

# Заполнить: Resource=Products, Fields=name,price,stock
# Запустить агентов согласно runbook
```

---

## Расширенные практики

### Мониторинг прогресса агентов

При параллельной работе нескольких агентов полезно отслеживать их прогресс.

**Простой способ — через git:**
```bash
# Каждые 2 минуты проверять статус
watch -n 120 '
  echo "=== Worktree A ===" && cd ../myproject-task-a && git status
  echo "=== Worktree B ===" && cd ../myproject-task-b && git status
'
```

**Progress Table в runbook:**

Создайте таблицу в runbook.md и обновляйте вручную или через агента:

```markdown
| Агент | Задача | Статус | Время | Проблемы |
|-------|--------|--------|-------|----------|
| A | Users API | 🔄 In progress | 15/20 мин | Нет |
| B | Logging | ✅ Done | 12 мин | Нет |
| C | Tests | ⬜ Pending | - | Зависит от A |
```

**Автоматический мониторинг через скрипт:**

```python
import subprocess
import time

worktrees = [
    ("../myproject-task-a", "Users API"),
    ("../myproject-task-b", "Logging"),
]

while True:
    for path, name in worktrees:
        result = subprocess.run(
            f"cd {path} && git status --short",
            shell=True, capture_output=True, text=True
        )
        if result.stdout:
            print(f"{name}: {len(result.stdout.splitlines())} changed files")
        else:
            print(f"{name}: Clean (возможно завершён)")
    time.sleep(120)  # Каждые 2 минуты
```

### Использование tmux для параллельной работы

tmux позволяет держать несколько сессий агентов в одном окне терминала:

```bash
# Создать новую tmux сессию
tmux new -s orchestration

# Разделить окно на 2 панели (вертикально)
Ctrl+b %

# Переключиться на левую панель
Ctrl+b ←

# В левой панели: запустить агента A
cd ../myproject-task-a && claude

# В правой панели: запустить агента B
cd ../myproject-task-b && claude

# Создать ещё одно окно для мониторинга
Ctrl+b c
watch -n 60 'git worktree list'

# Переключаться между окнами
Ctrl+b n  # next window
Ctrl+b p  # previous window

# Отключиться от сессии (агенты продолжат работать)
Ctrl+b d

# Подключиться обратно
tmux attach -t orchestration
```

## Приложение: Advanced темы

Краткие обзоры расширенных возможностей для самостоятельного изучения.

### Headless mode

Claude Code работает в headless-режиме для автоматизации:

```bash
# Запуск с промптом
claude -p "Добавь type hints во все функции в src/"

# Использование в CI/CD
claude -p prompts/fix-linting.md
```

Применение: CI/CD пайплайны, автоматизация рутинных задач, масштабирование на серверы.

**Пример использования в GitHub Actions:**

```yaml
name: Auto-add docstrings
on:
  schedule:
    - cron: '0 2 * * 1'  # Каждый понедельник

jobs:
  add-docstrings:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install Claude Code
        run: curl -fsSL https://claude.ai/install.sh | bash
      - name: Run agent
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: claude -p prompts/add-docstrings.md
      - name: Create PR
        run: |
          git checkout -b auto/docstrings-$(date +%Y%m%d)
          git add . && git commit -m "Auto-add docstrings"
          git push
```

### Auth токены

Для автоматизации используйте API ключи:

```bash
export ANTHROPIC_API_KEY="sk-ant-api03-YOUR_KEY"
claude -p "Generate tests"
```

Получить API key: console.anthropic.com/settings/keys. Никогда не коммитьте токены в git.

**Важно — безопасность:**

- Добавьте `.env` в `.gitignore`
- В CI/CD используйте secrets (GitHub Actions, GitLab CI)
- Для production: AWS Secrets Manager, HashiCorp Vault, Azure Key Vault

**Стоимость (pay-as-you-go):**
- Claude Sonnet 4.5: $1.75 input / $3.00 output за 1M tokens
- Claude Opus 4.6: $5.00 input / $21.00 output за 1M tokens
- Claude Haiku 4.5: $0.03 input / $1.00 output за 1M tokens

Альтернатива: Claude Max подписка ($100-200/мес).

### Claude Agent SDK

SDK для программной оркестрации (Python/TypeScript):

```bash
pip install anthropic  # Python
npm install @anthropic-ai/sdk  # TypeScript
```

Используйте для сложных workflow, интеграции с внешними системами, event-driven автоматизации.

**Пример использования (Python):**

```python
import os
from anthropic import Anthropic

client = Anthropic(api_key=os.environ["ANTHROPIC_API_KEY"])

tasks = [
    {"name": "add-logging", "prompt": "Add logging to all API endpoints"},
    {"name": "add-tests", "prompt": "Add unit tests for new endpoints"}
]

for task in tasks:
    response = client.messages.create(
        model="claude-sonnet-4-5-20250929",
        max_tokens=4096,
        messages=[{"role": "user", "content": task["prompt"]}]
    )
    print(f"Task {task['name']}: {response.content[0].text[:100]}...")
```

**Когда использовать:**
- Интеграция с task trackers (Jira, Linear, GitHub Issues)
- Event-driven automation (новый PR → auto-review)
- Custom orchestration (N агентов на N микросервисах)

**Когда хватает CLI:**
- Разовые задачи
- Простые workflows
- Эксперименты

Документация: docs.anthropic.com/claude/reference

### Agent Teams (experimental)

Экспериментальная координация нескольких агентов:

```bash
export CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1
claude
```

Возможности: shared task queue, inter-agent messaging, plan approval mode.

**Реальный кейс от Anthropic:**

16 параллельных агентов работали над Rust-компилятором проекта C. За 2000 сессий написали ~100K строк кода. Большинство PR приняты после минимального review.

**Проблемы:**
- Overhead на координацию (больше токенов)
- Риск циклических зависимостей
- Высокая стоимость ($200-300/день для 16 Sonnet агентов)

**Когда использовать:**
- Experimental проекты с большим бюджетом
- Крупные рефакторинги (миграция кодовой базы)
- Research проекты

**Когда НЕ использовать:**
- Production (пока experimental)
- Малый бюджет
- Простые задачи

Статус (Feb 2026): экспериментальный, API может измениться.

**Альтернативы для параллельной работы:**
- Git worktrees (проверенный подход, описан в Шаге 1)
- Программная оркестрация через SDK
- Enterprise платформы (см. ниже)

### Практические рекомендации по масштабированию оркестрации

**Когда использовать 2 агента:**
- Простые фичи (backend + frontend)
- Code + tests
- Implementation + review

**Когда использовать 3-4 агента:**
- Полноценные фичи (backend + frontend + tests + docs)
- Рефакторинг нескольких модулей
- Миграции (schema + data + code + tests)

**Когда использовать 5+ агентов:**
- Крупные features spanning multiple services
- Параллельная работа на микросервисах
- Mass refactoring (например, миграция с Python 2 на Python 3)

**Лимиты параллелизма:**

Практический предел: 5-7 агентов одновременно для одного человека.

Почему:
- Сложно мониторить > 7 сессий
- Overhead на координацию растёт exponentially
- Риск конфликтов увеличивается
- Стоимость ($3-10 за агента-час) суммируется

**Оптимальная стратегия:**
1. Начните с 2 агентов (generator + reviewer)
2. Переходите к 3-4 (параллельные tasks)
3. Масштабируйте до 5-7 только для очень крупных задач
4. При > 7 используйте программную оркестрацию (SDK, OpenClaw)

### Cost-benefit analysis

**Пример расчёта для фичи "Export to CSV":**

**Последовательное выполнение (1 агент):**
- Backend: 20 мин × $0.10/мин = $2.00
- Frontend: 20 мин × $0.10/мин = $2.00
- E2E: 15 мин × $0.10/мин = $1.50
- Итого: 55 минут, $5.50

**Параллельное выполнение (3 агента):**
- Backend + Frontend параллельно: 20 мин × 2 агента × $0.10 = $4.00
- E2E последовательно: 15 мин × $0.10 = $1.50
- Merge + troubleshooting: 5 мин × $0.10 = $0.50
- Итого: 40 минут, $6.00

**Результат:**
- Экономия времени: 15 минут (27%)
- Дополнительная стоимость: $0.50 (9%)
- ROI: Если ваше время стоит > $2/минут, параллелизация окупается

**Когда параллелизация НЕ окупается:**
- Простые задачи (< 30 минут общего времени)
- Сильно зависимые подзадачи (одна блокирует другую)
- Высокий риск конфликтов (агенты работают с одними файлами)

---

## Итоги упражнений

Вы прошли путь от базовой изоляции через git worktrees до расширенных техник оркестрации.

**Что вы освоили:**

1. **Git worktree** — изоляция агентов для параллельной работы
2. **Генератор + Ревьюер** — двухэтапная проверка для повышения качества
3. **Параллельная работа** — два агента на независимых задачах
4. **Слияние результатов** — merge веток и разрешение конфликтов
5. **Runbook** — формализованный план для оркестрации агентов

**Бонусные темы:**

- **Headless-режим** — автоматизация через CLI и CI/CD
- **Auth-токены** — долговременные токены для автоматизации
- **Agent SDK** — программная оркестрация на Python/TypeScript
- **Agent Teams** — экспериментальная координация агентов
- **OpenClaw** — платформа для enterprise-уровня автоматизации

**Следующие шаги:**

1. **Практикуйте на реальных проектах.** Возьмите фичу из вашего проекта и реализуйте её с использованием 2-3 агентов параллельно.

2. **Создайте библиотеку runbook'ов.** Для типовых задач (добавить endpoint, создать CRUD, написать тесты) создайте готовые runbook'и.

3. **Экспериментируйте с профилями агентов.** Попробуйте разные модели (Sonnet vs Haiku vs Opus) на разных типах задач. Измеряйте время и стоимость.

4. **Автоматизируйте рутину.** Настройте headless-агентов для регулярных задач (обновление зависимостей, генерация документации, исправление linting errors).

5. **Изучите теорию оркестрации.** Прочитайте Модуль 7 (theory.md) для понимания концепций: Ralph Loop, ролевая модель, паттерны оркестрации, мониторинг.

**Рефлексия:**

Ответьте себе на вопросы:
- Какие упражнения были самыми полезными?
- Где вы можете применить параллельную работу агентов в ваших проектах?
- Какие проблемы возникли и как вы их решили?
- Стоит ли overhead на координацию затраченных усилий?

**Помните:** Оркестрация — это не о замене разработчика агентами. Это о переходе от роли "я пишу код" к роли "я проектирую систему, которая пишет код". Вы становитесь архитектором оркестра, менеджером процессов, контролёром качества.

Первое время это сложнее и медленнее. Но когда система настроена, вы получаете производительность команды из 5-10 человек, работая в одиночку или с небольшой командой.

---

## Troubleshooting: Распространённые проблемы

### Worktree конфликты при создании

**Проблема:** `fatal: 'myproject-feature' already exists`

**Решение:**
```bash
# Посмотреть существующие worktrees
git worktree list

# Удалить старый worktree
git worktree remove ../myproject-feature

# Или принудительно если git worktree remove не работает
git worktree remove --force ../myproject-feature
```

### Агент не видит изменения из другого worktree

**Проблема:** Агент A работает в worktree A, агент B в worktree B. Агент B не видит изменения агента A.

**Причина:** Это нормально — worktrees изолированы до момента merge.

**Решение:** Если агент B зависит от изменений агента A:
1. Агент A должен закоммитить изменения
2. Агент B должен сделать merge из ветки агента A
3. Или работайте последовательно, а не параллельно

### Тесты падают после merge

**Проблема:** Каждый агент прошёл свои тесты, но после merge общие тесты падают.

**Причины:**
- Несовместимые изменения (агент A изменил API, агент B использует старый формат)
- Дублирование функций (оба добавили функцию с одинаковым именем)
- Конфликты в импортах

**Решение:**
```bash
# Запустить полный набор тестов после merge
pytest  # или npm test, go test
# Анализировать ошибки
# Исправить вручную или через агента
```

### API ключ не работает в headless режиме

**Проблема:** `Error: ANTHROPIC_API_KEY not set`

**Решение:**
```bash
# Проверить что переменная установлена
echo $ANTHROPIC_API_KEY

# Если пусто — добавить в ~/.bashrc
echo 'export ANTHROPIC_API_KEY="sk-ant-api03-YOUR_KEY"' >> ~/.bashrc
source ~/.bashrc

# Проверить снова
echo $ANTHROPIC_API_KEY
```

### Агент застрял в бесконечном цикле

**Симптомы:** Агент постоянно пытается исправить один и тот же тест, но ничего не меняется.

**Решение:**
1. Прервать агента: Ctrl+C
2. Посмотреть что не так (логи, ошибка теста)
3. Дать более конкретный промпт:

```
Вместо: "Исправь падающий тест"

Используйте:
"Тест test_user_login падает с ошибкой KeyError: 'token'.
Причина: функция login() не возвращает ключ 'token' в словаре.
Исправь: добавь 'token': generate_token(user) в return statement."
```

### Конфликты слишком сложные для автоматического разрешения

**Проблема:** Агент-арбитр не может разрешить конфликт или делает это неправильно.

**Решение:** Разрешите конфликт вручную:
1. Откройте файл в редакторе
2. Найдите секции `<<<<<<< HEAD` и `>>>>>>> branch`
3. Объедините изменения логически (не просто удалите маркеры)
4. Сохраните, добавьте в git, закоммитьте
5. Запустите тесты для проверки

**Когда разрешать вручную:**
- Сложная бизнес-логика (требуется понимание контекста)
- Архитектурные решения (какой подход лучше)
- Конфликты в критичных местах (безопасность, платежи)

**Когда можно использовать агента:**
- Тривиальные конфликты (импорты, форматирование)
- Повторяющиеся паттерны (добавление полей в модель)
- Документация и комментарии

---


## Практические кейсы из реальных проектов

### Кейс 1: Миграция монолита на микросервисы

**Задача:** Выделить модуль аутентификации из монолита в отдельный сервис.

**Runbook approach:**

```markdown
## Tasks (5 агентов параллельно)

1. **Extract service** (Agent A)
   - Создать новый репозиторий auth-service
   - Скопировать код аутентификации
   - Настроить зависимости

2. **Create API** (Agent B)
   - REST API для login/logout/verify
   - OpenAPI spec
   - API tests

3. **Update monolith** (Agent C)
   - Заменить прямые вызовы на HTTP calls
   - Обновить тесты
   - Добавить retry logic

4. **Database migration** (Agent D)
   - Вынести users таблицу в отдельную БД
   - Миграция данных
   - Rollback plan

5. **Infrastructure** (Agent E)
   - Dockerfile для auth-service
   - Kubernetes manifests
   - CI/CD pipeline

## Execution

Week 1: Agents A, B, C параллельно (независимые)
Week 2: Agent D (требует A+B+C completed)
Week 3: Agent E (требует D completed)
```

**Результат:**
- Без оркестрации: 3 недели последовательной работы
- С оркестрацией: 3 недели, но первая неделя сжата в 2 дня
- Экономия: ~8 рабочих дней

### Кейс 2: Добавление feature flag системы

**Задача:** Внедрить feature flags во все фичи приложения.

**Approach:**

```markdown
## Phase 1: Infrastructure (1 агент)
- Feature flag service (LaunchDarkly / Unleash)
- SDK integration
- Admin UI

## Phase 2: Wrapping features (10 агентов параллельно)
- Agent 1-10: каждый оборачивает 5-7 фич в feature flags
- Worktree на каждого агента
- Независимые ветки

## Phase 3: Testing (3 агента)
- Agent A: Unit tests
- Agent B: Integration tests
- Agent C: E2E tests (флаги on/off)

## Merge strategy
- Merge Phase 1 → main
- Merge all Phase 2 agents (resolve conflicts in parallel)
- Merge Phase 3 → main
```

**Результат:**
- 100+ фич обёрнуты в feature flags за 2 дня вместо 2 недель
- Конфликты минимальны (разные файлы)
- Automated testing покрывает все комбинации

### Кейс 3: Security audit и fixing

**Задача:** Провести security audit и исправить все найденные проблемы.

**Orchestration:**

```markdown
## Step 1: Audit (1 специализированный агент)
Промпт:
"Проведи security audit проекта:
1. Найди SQL injections
2. XSS vulnerabilities
3. Hardcoded secrets
4. Missing authorization checks
5. Insecure dependencies

Создай файл SECURITY-ISSUES.md с полным списком проблем, категоризированных по severity (critical/high/medium/low)."

## Step 2: Fixing (N агентов параллельно)
Для каждого critical/high issue:
- Создать worktree
- Агент исправляет проблему
- Агент пишет тест что проблема исправлена
- Агент создаёт PR

## Step 3: Verification (1 агент-верификатор)
- Re-run security scan
- Проверить что все critical/high закрыты
- Создать отчёт
```

**Результат:**
- 45 security issues найдены за 20 минут
- 45 PR созданы параллельно за 3 часа (вместо недели ручной работы)
- 100% критичных проблем закрыты

## Метрики и KPI для оркестрации

Отслеживайте эффективность оркестрации:

### Time metrics

```markdown
# Tracker template
| Task | Sequential time | Parallel time | Agents count | Speedup |
|------|-----------------|---------------|--------------|---------|
| CSV export | 60 min | 35 min | 3 | 1.7x |
| Auth refactor | 3 days | 2 days | 5 | 1.5x |
| Security fixes | 1 week | 3 hours | 45 | 18.7x |
```

### Cost metrics

```markdown
| Task | Manual cost | Agent cost | Savings |
|------|-------------|------------|---------|
| CSV export | $100 (dev 1h @ $100/h) | $6 (agents) | $94 |
| Security fixes | $4000 (dev 1w @ $100/h) | $150 (agents) | $3850 |
```

### Quality metrics

```markdown
| Metric | Before orchestration | After orchestration |
|--------|---------------------|---------------------|
| Bugs per release | 15 | 5 | 
| Test coverage | 65% | 85% |
| Time to review PR | 2 days | 30 min |
| Deployment frequency | 1x/week | 3x/week |
```

## Troubleshooting: Advanced проблемы

### Circular dependencies между агентами

**Проблема:** Агент A ждёт результат от агента B, агент B ждёт результат от агента A.

**Причина:** Неправильное планирование dependencies в runbook.

**Решение:**
1. Идентифицируйте dependency graph
2. Найдите циклы (A → B → A)
3. Разбейте один из узлов на 2 части:
   ```
   БЫЛО: A → B → A
   СТАЛО: A1 → B → A2
   ```
4. A1 делает минимум нужный для B, B завершается, A2 доделывает остальное

### Race conditions в shared resources

**Проблема:** Два агента изменяют один конфиг файл одновременно, результат непредсказуем.

**Решение:**
1. **Lock files:** Используйте git branch locks
   ```bash
   # Агент A
   git checkout -b feat/config-changes-lock
   # [работа с конфигом]
   git commit && git push

   # Агент B видит что ветка существует, ждёт merge
   ```

2. **Sequential ordering:** В runbook укажите explicit ordering
   ```yaml
   tasks:
     - id: config-update-1
       blocks: [config-update-2, config-update-3]
   ```

3. **Split config:** Разделите конфиг на модули
   ```
   config/
     auth.yaml    # Агент A
     api.yaml     # Агент B
     db.yaml      # Агент C
   ```

### Token limits exceeded

**Проблема:** Агент достиг лимита токенов (200K context), не может продолжить.

**Решение:**
1. **Chunking:** Разбейте задачу на более мелкие
   ```
   БЫЛО: "Refactor all 50 files in src/"
   СТАЛО: 5 агентов × "Refactor 10 files each"
   ```

2. **Summary pattern:** Агент пишет summary после каждого шага
   ```python
   # Agent prompt
   After each file:
   1. Refactor file
   2. Write 1-sentence summary
   3. Append to PROGRESS.md
   4. Clear context (forget previous files)
   ```

3. **Incremental approach:**
   ```
   Agent iteration 1: Files 1-10
   Agent iteration 2: Files 11-20 (starts fresh)
   ...
   ```

## Заключительные советы

**Начните с малого:**
- 1 агент → 2 агента (generator+reviewer) → 3-4 параллельных → масштабируйте постепенно

**Инвестируйте в runbook'и:**
- Первый раз медленно и подробно
- Второй раз по runbook быстрее
- Третий раз автоматизировано через SDK

**Измеряйте всё:**
- Время, стоимость, качество
- Не полагайтесь на ощущения
- Data-driven decisions

**Не переоптимизируйте:**
- Параллелизация 2-минутной задачи не имеет смысла
- Overhead на координацию может превысить выигрыш
- Используйте правильный инструмент для задачи

**Развивайте культуру:**
- Делитесь runbook'ами с командой
- Проводите ретроспективы оркестрации
- Обучайте коллег best practices

Оркестрация — это skill который развивается с практикой. Первые попытки будут медленными, но с опытом вы научитесь эффективно координировать агентов и значительно ускорите разработку.
